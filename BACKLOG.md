# BACKLOG

Alleinige Quelle der Wahrheit für den Aufgabenstand. Workflow-Regeln siehe `CLAUDE.md`.

## Offen

- **Flaky UI-Test stabilisieren** – nicht auf einen einzelnen Test beschränkt: `testGymConversionFactorPersistsAfterEditing` schlug einmal bei einem vollen Suite-Lauf fehl (isoliert und in späteren Läufen grün); während des Design-Polishing-Tickets schlugen in einem einzelnen vollen 23-Test-Lauf sogar 8 thematisch unzusammenhängende UI-Tests gleichzeitig fehl (Gym-, Plan- und Workout-Screens gemischt) und liefen isoliert sowie im direkt darauffolgenden vollen Lauf wieder komplett grün; während des Einarmig/einseitig-Tickets schlug `testUnlockingDeveloperModeViaVersionTapShowsDevModeSection` in einem Teillauf mit einem simulator-seitigen "Timed out while synthesizing event" beim Tap fehl (kein App-/Assertion-Fehler) und lief isoliert sofort wieder grün – deutet weiterhin stark auf Ressourcen-Konkurrenz des Simulators bei voller Suite hin, nicht auf einen Code-Bug. Kein Produktionsbug, aber wert, die Suite robuster gegen diese Art von Lastspitzen zu machen (z. B. `doubleTap()`-Interaktionen, Wartezeiten), falls es sich weiter häuft.

## In Arbeit

## Erledigt

- **SetRow-Politur** – Nutzer-Feedback: zu viel Leerraum zwischen Kennung/Gewicht und zwischen "×"/Wdh., Felder wirken zu klein/minimalistisch, der Erledigt-Kreis rechts hatte keine Tap-Funktion. *(2026-08-19)*
  - Gebaut: Ursache der Lücken war, dass `.frame(minWidth:)` allein nur eine Untergrenze setzt – die Gewicht-/Wdh.-TextFields akzeptierten weiterhin die vom HStack angebotene, oft viel größere Breite, was bei `.trailing`-Ausrichtung als Leerraum links der Ziffern sichtbar wurde. Behoben durch zusätzliches `.fixedSize(horizontal: true, vertical: false)`, das die Felder an ihren Inhalt anschmiegen lässt statt zu wachsen – `minWidth` bleibt dabei eine echte Untergrenze, kein Cap, also weiterhin kein Abschneide-Risiko bei großer Dynamic-Type-Schrift (Nulltoleranz-Regel aus dem Design-Polishing-Ticket bleibt gewahrt). Schriftgrößen angehoben: Gewicht/Wdh. von implizitem `.body` auf `.title3.weight(.semibold)`, "kg ×"-Trenner von `.caption2` auf `.subheadline`, Kennung von `.subheadline` auf `.body.weight(.semibold)`. Der Erledigt-Kreis ist jetzt ein echter `Button` (`.buttonStyle(.borderless)`, analog zu allen anderen Buttons in dieser HStack-in-List-Situation), der dieselbe `onToggleCompleted`-Closure aufruft wie die bestehende Swipe-Aktion (jetzt zusätzlich in `SetRow` durchgereicht) – keine doppelte Logik. Tap-Fläche per `.contentShape(Rectangle().inset(by: -8))` über die sichtbare Symbolgröße hinaus vergrößert (Review-Fund, s. u.).
  - **Nicht-triviale Nebenwirkung gefunden und behoben**: `.fixedSize()` auf dem Gewichtsfeld brach den bestehenden UI-Test `testSwipingASetTogglesErledigtManually` – das Feld dient als "Adresse" der Zeile für die Swipe-Aktion (dokumentiert an anderer Stelle in der Datei), und XCUITests `swipeRight()` zieht seine Wegstrecke aus dem eigenen Rahmen des Zielelements. Bei `minWidth: 40` war das auf Inhaltsgröße geschrumpfte Feld offenbar zu schmal, damit die Geste noch als Swipe-Reveal statt als Tap erkannt wurde (empirisch bestätigt: ohne `fixedSize` lief der Test wieder grün). Behoben durch Anheben von `minWidth` auf 70 bei weiterhin aktivem `fixedSize` – Test seither zuverlässig grün.
  - Getestet: 1 neuer XCUITest `testTappingTheCompletionCircleTogglesErledigt` (Antippen schaltet um, erneutes Antippen zurück), nutzt den bestehenden `tapHittableButton`-Helfer statt eines rohen `.tap()` (Review-Fund, s. u.). Gesamt-Unit-Suite (333 Tests) unverändert grün. Gezielter Batch-Lauf aller 8 neuen/geänderten UI-Tests dieses und des parallel gebauten Gym-Tickets grün (s. Notiz unten zu den unterbrochenen vollständigen Suite-Läufen).
  - Unabhängiger Review-Pass: APPROVE WITH NOTES, keine kritischen/hohen Funde.
    - **Mittel, behoben**: neuer Test nutzte rohes `.tap()` statt des etablierten `tapHittableButton`-Helfers, obwohl die Zeile (wegen der eigenen `.swipeActions`) für mehrdeutige Accessibility-Elemente bekannt ist – umgestellt.
    - **Niedrig, behoben**: Tap-Fläche des Erledigt-Buttons (blankes SF-Symbol bei `.title3`, ~20pt) lag unter Apples 44×44pt-Richtwert – per `.contentShape(Rectangle().inset(by: -8))` vergrößert, ohne optisch größer zu wirken oder ins Notiz-Feld hineinzuragen.
    - **Niedrig, bewusst nicht verändert**: Da Gewicht/Wdh. jetzt `fixedSize` sind, fängt allein das Notiz-Feld den gesamten Platzdruck der Zeile bei extremen Accessibility-Textgrößen ab – verletzt laut Reviewer die Abschneide-Regel nicht, eine visuelle AX5-Stichprobe steht als Nice-to-have aus.
  - **Wichtiger Nebenbefund während der Verifikation (kein Code-Bug)**: Ein vergessener paralleler Hintergrund-Testlauf griff mehrfach zeitgleich mit neuen `xcodebuild`-Aufrufen auf denselben Simulator/dieselbe DerivedData zu und verursachte dadurch mehrere App-Abstürze, die zunächst wie eine echte Regression aussahen (u. a. ein vermuteter SwiftData-Reactivity-Loop, der sich im Review als unbegründet herausstellte – siehe Gym-Ticket unten). Aufgeklärt per `ps aux`, Prozess beendet, danach sauber reproduzierbar. Ein späterer vollständiger Suite-Lauf wurde zusätzlich durch eine fremde, auf demselben Simulator installierte App (Restock), die kurzzeitig in den Vordergrund wechselte, unterbrochen – ebenfalls kein GymTrack-Bug, der betroffene Test lief isoliert sofort wieder grün.

- **Gym-Auswahl direkt unter "Pläne"** – Nutzer-Feedback: Gym-Wechsel bisher nur über Einstellungen erreichbar, zu umständlich; sollte direkt im Pläne-Tab zwischen Gyms (oder "Alle") umschaltbar sein, Pläne sollen ihr Gym anzeigen. *(2026-08-19)*
  - Gebaut: neues `TrainingPlan.gym: Gym?` (optional, kein `deleteRule:` → nullify wie bei `SetEntry`/`WorkoutSession`/`PersonalRecord`, Inverse auf `Gym.plans`) – ein Plan ohne Gym gilt als "überall einsetzbar". Neue reine `PlanGymFiltering.filter(_:byGymID:)`: `nil` zeigt alle Pläne, ein Gym zeigt dessen Pläne PLUS gymlose Pläne, eine nicht mehr existierende Gym-ID zeigt nur die gymlosen (kein Sonderfall nötig, fällt aus der Logik heraus). `PlanListView` bekommt einen Menu-Picker in der Toolbar (`.topBarLeading`, `.pickerStyle(.menu)`, immer strukturell vorhanden + `.disabled(gyms.isEmpty)` statt bedingt eingefügt – analog zum bereits dokumentierten Overflow-Vermeidungsmuster in `PlanEditorView`) mit "Alle" + jedem Gym; Auswahl eines Gyms aktiviert es (`GymActivation`, wie bisher in Einstellungen → Gyms) UND filtert die Liste, "Alle" ist ein reiner Anzeigefilter und rührt das aktive Gym nicht an. Einmalige Erstbefüllung des Filters aus dem aktuell aktiven Gym per `.task` (gegen ein `hasInitializedGymFilter`-Flag abgesichert, da `nil` sowohl "noch nicht initialisiert" als auch "Alle" bedeutet). `PlanRow` zeigt den Gym-Namen in der Caption. Neue Pläne (leer wie aus Vorlage) übernehmen automatisch den aktuell gewählten Filter; `TrainingPlanDuplication` übernimmt das Gym des Quellplans; `PlanExportImport` exportiert/importiert das Gym per Namens-Match (`PlanExport.gymName: String?`, kein Treffer → Plan bleibt trotzdem gültig, nur ohne Gym – wie beim bestehenden Übungs-Namens-Match). `PlanEditorView` bekommt einen eigenen Picker zum nachträglichen Setzen/Ändern/Entfernen des Gyms (bindet direkt an `$plan.gym`, nicht per ID-Lookup wie im Pläne-Tab – bewusst unterschiedlich, da hier die tatsächliche Relationship editiert wird statt transienter Tab-UI-State ohne eigenes Model, analog zum bereits etablierten `StatisticsTabView.selectedExercise`-Muster).
  - Getestet: 5 neue Unit-Tests `PlanGymFilteringTests` (nil zeigt alles, spezifisches Gym zeigt Treffer+gymlose, verwaiste ID zeigt nur gymlose, leere Liste, Reihenfolge bleibt erhalten), 2 neue `TrainingPlanDuplicationTests` (Gym wird übernommen bzw. bleibt leer), 7 neue `PlanExportImportTests` (Export mit/ohne Gym, Encode/Decode-Rundtrip, altes JSON ohne `gymName`-Schlüssel dekodiert sauber zu `nil`, `makePlan` matcht per Namen bzw. lässt `nil` bei keinem Treffer), 1 neuer `ModelTests`-Fall (Löschen eines Gyms nullt `TrainingPlan.gym`, löscht den Plan aber nicht). 4 neue XCUITests: Filter wechselt aktives Gym + filtert Liste, Gym-Zuweisung im Plan-Editor, Erstbefüllung des Filters aus dem aktiven Gym beim allerersten Öffnen des Tabs, Löschen des aktiven/gefilterten Gyms folgt der automatischen Ersatz-Beförderung (`GymActivation.promoteReplacement`) statt auf "Alle" zurückzufallen. Gesamt 333 Unit-Tests grün; alle 8 neuen/geänderten UI-Tests dieses und des parallel gebauten SetRow-Tickets in einem gezielten Batch-Lauf grün (s. Notiz dort zu den unterbrochenen vollständigen Suite-Läufen).
  - Unabhängiger Review-Pass: APPROVE WITH NOTES, keine kritischen/hohen Funde.
    - **Niedrig, behoben**: ein zwischenzeitlich entferntes `.onChange(of: plan.gym) { plan.updatedAt = .now }` (aus Vorsicht vor einem vermuteten Reactivity-Loop entfernt, der sich als Fehldiagnose herausstellte – die eigentliche Ursache des zwischenzeitlichen Absturzes war der parallele Hintergrund-Testlauf, s. o.) wieder ergänzt: `@Model`s synthetisiertes `Equatable` vergleicht über die `persistentModelID`, ein reiner `updatedAt`-Schreibzugriff kann den `onChange` also nicht erneut auslösen – exakt dasselbe, bereits bestehende Muster wie bei `plan.name`. Ohne diesen Fix wäre eine reine Gym-Änderung die einzige Bearbeitung dieser View gewesen, die den Plan nicht in der `updatedAt`-sortierten Liste nach oben holt.
    - **Mittel, behoben**: `.onChange(of: gyms)` fiel beim Löschen des gerade gefilterten Gyms bedingungslos auf "Alle" zurück – dabei befördert `GymActivation.promoteReplacement` beim Löschen des AKTIVEN Gyms bereits automatisch ein Ersatz-Gym, und der Pläne-Filter driftete dadurch unbemerkt vom tatsächlich aktiven Gym auseinander. Behoben: der Filter folgt jetzt stattdessen dem neu aktiven Gym (fällt nur auf "Alle" zurück, wenn wirklich kein Gym mehr aktiv ist) – mit eigenem Regressionstest abgesichert.
    - **Niedrig, behoben**: unnötiger No-op-Save beim ersten Öffnen des Tabs, weil die `.task`-Erstbefüllung von `selectedGymID` ihr eigenes `onChange` erneut auslöste und ein bereits aktives Gym redundant reaktivierte – durch eine `!gym.isActive`-Kurzschluss-Bedingung vermieden.
    - Bestätigt: Nullify-Delete-Verhalten exakt deckungsgleich mit dem bereits bestehenden Muster (`SetEntry`/`WorkoutSession`/`PersonalRecord`/`ExerciseGymNote`/`ExerciseGymReminder`); alle Plan-Erstellungspfade korrekt aufgeteilt (neu/aus Vorlage übernehmen den Filter, Duplizieren/Import bewusst nicht – übernehmen stattdessen das Gym der Quelle); Direkt-Binding im Editor vs. ID-Lookup im Pläne-Tab als bewusste, begründete Unterscheidung bestätigt statt als Inkonsistenz.
  - Eigener Testfehler bei der Verifikation gefunden und behoben (kein Produktionsbug): der neue Plan-Editor-Test suchte den Gym-Picker über `.accessibilityIdentifier`, die View hatte aber nur ein `.accessibilityLabel` gesetzt – Identifier ergänzt. Der Filter-Test prüfte ursprünglich erst ganz am Ende, ob "Berlin" noch aktiv sei, obwohl der Test zwischenzeitlich bewusst auf "Frankfurt" umschaltet (was Frankfurt korrekt aktiviert, nicht Berlin) – die Prüfung direkt nach der Berlin-Auswahl vorgezogen, um tatsächlich das zu testen, was sie sollte.

- **Langfristige Ziele je Übung (Ziel-Wdh./Ziel-Gewicht)** – Pro Übung lässt sich ein langfristiges Ziel setzen (Ziel-Wiederholungen und/oder Ziel-Gewicht, unabhängig voneinander), mit Fortschritts-Visualisierung (aktueller Bestwert, was noch bis zum Ziel fehlt, "Ziel erreicht"). *(2026-08-16)*
  - Gebaut: neues `ExerciseGoal`-Model (`targetWeight`/`targetReps` je optional, 1:1 mit `Exercise` über `ExerciseGoalStore.findOrCreate`, analog zum bestehenden `ExerciseGymNote`-Muster). Reine Business-Logik `GoalMetricProgress` (Fortschritts-Bruch/Rest-bis-Ziel/"erreicht"-Status, auf [0,1] geklemmt, nicht-positives Ziel defensiv als 0 behandelt) + `GoalProgress` (aktueller Bestwert aus derselben `SessionStatistics.eligibleSessions`-Historie wie bei `PersonalRecordFinder`, nur `isCompleted && .normal`-Sätze). Bewusst nicht gym-konvertiert (anders als `PersonalRecordFinder`) – einfaches persönliches Ziel, kein gym-übergreifender Vergleich; siehe Review-Fund unten für die Kehrseite dieser Entscheidung. Neue `ExerciseDetailView`, von `ExerciseLibraryView` aus erreichbar (Zeilen dort waren zuvor nirgends tappbar) – editierbare Zielfelder mit Autosave bei jeder Eingabe (kein separater Speichern-Button, da Push- statt Sheet-Navigation) + `ProgressView`-Visualisierung pro tatsächlich gesetztem Ziel.
  - **Nicht-triviale Navigations-Falle gefunden**: `NavigationLink(value:)` + `.navigationDestination(for:)` – exakt das Muster, das in `PlanListView`/`TrainingTabView` bereits produktiv funktioniert – hat für den neuen Tap-Übung-öffnet-Detailansicht-Flow trotz mehrerer plausibler Korrekturversuche (Ziel-Modifier auf verschiedene Vorfahren verschoben, `.accessibilityElement(children: .combine)` von der Zeile entfernt, `.onDelete` durch `.swipeActions` ersetzt, explizites `NavigationPath`-Binding ergänzt) nie ausgelöst – per Bildschirmaufnahme empirisch bestätigt (Tap registriert, aber keinerlei Navigation). `ExerciseLibraryView` wird selbst über einen wertlosen `NavigationLink { ... }` von `SettingsView` aus gepusht; die Vermutung ist eine Inkompatibilität zwischen wertlosem und wertbasiertem Pushen innerhalb desselben Stacks, nicht abschließend verifiziert. Behoben durch Rückkehr zum einfachen, wertlosen `NavigationLink { ExerciseDetailView(exercise:) } label: { ... }` – demselben Muster, mit dem "Gyms"/"Übungen" selbst bereits zuverlässig gepusht werden.
  - Getestet: `GoalMetricProgressTests` (Randfälle: kein Bestwert, Ziel exakt erreicht/überschritten, nicht-positives Ziel crasht nicht) + `GoalProgressTests` (Aufwärm-/Dropsätze und unerledigte Sätze ausgeschlossen, laufende Sessions ausgeschlossen, andere Übungen ausgeschlossen, echte 0 vs. "keine Historie" unterschieden) + `ExerciseGoalStoreTests` (Dedup analog `ExerciseGymNoteStoreTests`) + `ModelTests`-Ergänzung für den Cascade-Delete von `ExerciseGoal` beim Löschen der Übung. 1 neuer XCUITest `testExerciseGoalShowsProgressTowardLoggedBest` (Satz loggen → Ziel drüber setzen zeigt "Aktuell"/"Noch" → Ziel drunter setzen zeigt "Ziel erreicht") + `testSwipingAnExerciseDeletesItFromTheLibrary`. Gesamt 318 Unit-Tests + 38 UI-Tests grün.
  - Unabhängiger Review-Pass: APPROVE WITH NOTES, keine kritischen/hohen Funde.
    - **Mittel, bewusst akzeptiert**: die Nicht-Gym-Konvertierung kann bei einem Nutzer mit mehreren unterschiedlich kalibrierten Gyms ein irreführendes "Aktuell"/"Ziel erreicht" zeigen (ein an einem leichter kalibrierten Gym geloggter Satz zählt unverändert mit). Bewusste, dokumentierte Abwägung passend zum einfachen "und/oder"-Charakter des ursprünglichen Wunsches, keine stille Lücke.
    - **Mittel, behoben**: fehlender Cascade-Delete-Test für `ExerciseGoal` trotz Verhalten selbst schon korrekt (Reviewer hat das per temporärem Test empirisch bestätigt) – nachgezogen nach demselben Muster wie die bestehenden `ExerciseGymNote`/`ExerciseGymReminder`-Cascade-Tests.
    - **Niedrig, behoben**: veralteter Kommentar in `testAddingCustomExerciseShowsItInLibrary` (verwies noch auf ein inzwischen entferntes `.accessibilityElement(children: .combine)`); fehlender Swipe-Delete-Regressionstest für Übungen (ergänzt); fehlender Test für die 0-vs-kein-Bestwert-Grenze in `GoalProgress` (ergänzt) – exakt dieselbe Bugklasse wie beim Ghost-Werte-Mechanismus des vorherigen Tickets, daher als günstige Absicherung ergänzt statt übergangen.
  - **Schwerwiegender Zwischenfall während des Reviews, vollständig aufgeklärt und behoben**: Der Review-Subagent führte zur Verifizierung eigene temporäre Tests in `GymTrackUITests.swift` ein und rief anschließend `git checkout -- GymTrackUITests/GymTrackUITests.swift`, um nur seine eigene Änderung rückgängig zu machen – das hat aber die GESAMTE Datei auf den letzten Commit zurückgesetzt und damit sämtliche noch nicht committeten UI-Test-Ergänzungen dieser Session gelöscht (Entwicklermodus-, Trainingsansicht-neu- und Einarmig/einseitig-Tickets, insgesamt von 26 auf vorher 37 Tests). Aufgeklärt durch gezielte Auswertung des Subagent-Transkripts (JSONL): frühere `Read`/`grep`-Aufrufe desselben Subagenten enthielten noch unbeschädigte Ausschnitte der Datei, u. a. den kompletten, bereits verifiziert grünen `testExerciseGoalShowsProgressTowardLoggedBest` sowie den Anfang von `testUnilateralExerciseAlternatesLeftAndRightSetLabels` – beide wortwörtlich daraus wiederhergestellt. Alle übrigen verlorenen Tests (Entwicklermodus-Flows, die komplette Sheet→Inline-Umstellung der Trainingsansicht-Tests, `swipeHittableElement`-Hilfsfunktion, neue Ghost-Werte-/manueller-Start-/Übungsansicht-Tests) wurden anhand des tatsächlichen aktuellen App-Codes (`WorkoutSessionView`, `PlanStartView`, `TrainingTabView`, `DeveloperModeView` u. a. direkt gelesen, nicht aus der Erinnerung geraten) neu geschrieben und Zeile für Zeile gegen echte Testläufe verifiziert – dabei zwei eigene Rekonstruktionsfehler gefunden und behoben (ein Test nahm fälschlich an, `SetSuggestion.suggest` würde auch ohne aktives Gym greifen; zwei Tests prüften fälschlich das Ende des gesamten Workouts statt gezielt den Swipe-Status der einen betroffenen Zeile, obwohl die anderen Standard-Sätze des Plans absichtlich unvollständig blieben). Abschließend zweimal die komplette Suite (Unit + alle UI-Tests) frisch durchlaufen lassen, beide Male vollständig grün, kein Datenverlust im Endergebnis.

- **Einarmige/einseitige Übungen** – Übung als "einarmig/einseitig" markierbar. Bei Aktivierung wird die Satzzahl pro Seite gezählt statt gesamt (z. B. 3 Sätze pro Arm statt 6 Sätze gesamt), inkl. Kennzeichnung je Satz, welcher Seite (links/rechts) er zugeordnet ist. *(2026-08-14)*
  - Gebaut: neues `ExerciseSide`-Enum (`left`/`right`, `opposite`) + `SetEntry.side` (optional, Raw-Value-String-Pattern wie alle anderen gespeicherten Enums). `PlanExercise.isUnilateral: Bool = false` (pro Plan-Verwendung, nicht auf `Exercise` selbst – dieselbe Übung kann in einem Plan beidarmig, im nächsten einarmig sein, analog zu `targetWeight`/`note`). Neue geteilte `UnilateralSetPlan.sides(targetSets:isUnilateral:)`-Hilfsfunktion (bei deaktiviertem Flag `targetSets`× `nil`, sonst `2× targetSets` alternierend L/R – Satz 1 links, Satz 1 rechts, Satz 2 links, … statt erst alle links dann alle rechts) – von `WorkoutSessionBuilder` UND der `PlanStartView`-Vorschau gemeinsam genutzt, damit beide nie unabhängig voneinander aus dem Takt geraten können. `WorkoutRepetition` kopiert `side` unverändert vom Quell-Satz. `SetKennung` zählt normale Sätze pro Seite unabhängig ("1 L", "1 R", "2 L", …), Aufwärm-/Dropsätze bleiben unverändert "A"/"D". `PlanExerciseEditView` bekommt einen "Einarmig/einseitig"-Umschalter, das Sätze-Stepper-Label wechselt dabei zu "Sätze pro Seite"; `PlanEditorView`s Übungszeile hängt "pro Seite" an die Zusammenfassung an.
  - Getestet: neue `ExerciseSideTests` (`opposite`-Paarung) und `UnilateralSetPlanTests` (bilateral → alle `nil`, unilateral verdoppelt und alterniert, 1 Satz → genau 1×L+1×R, 0 und negative Sätze crashen nicht und liefern eine leere Sequenz). `SetKennungTests` um 2 Fälle erweitert (Kennungen pro Seite unabhängig gezählt). `WorkoutSessionBuilderTests` um 3 Fälle erweitert (nicht-einarmige Übungen bekommen `side == nil`, einarmige Übungen verdoppeln und alternieren in der richtigen Reihenfolge, beide Seiten teilen sich denselben Gewichts-/Wdh.-Vorschlag). `WorkoutRepetitionTests` um 1 Fall erweitert (`side` wird vom Quell-Satz übernommen). 1 neuer XCUITest `testUnilateralExerciseAlternatesLeftAndRightSetLabels` (Übung im Plan-Editor auf einarmig umschalten → Zusammenfassung zeigt "pro Seite" → Trainings-Vorschau → laufende Session zeigt für die ersten beiden Sätze exakt die erwarteten Kennung-Labels "1 L"/"1 R" inkl. der gesprochenen Seiten-Angabe). Gesamt 293 Unit-Tests + 36 UI-Tests grün (Suite wie üblich wegen des 10-Minuten-Limits pro Testlauf in drei Teilläufen à 12 UI-Tests abgesichert; ein einzelner Fehlschlag durch simulator-seitige Ressourcen-Konkurrenz, siehe Notiz unter "Offen" – isoliert sofort wieder grün, kein Zusammenhang mit diesem Ticket).
  - Beim abschließenden vollen Testlauf zusätzlich ein Bug im eigenen neuen UI-Test gefunden und behoben (kein Produktionsbug): `testUnilateralExerciseAlternatesLeftAndRightSetLabels` nahm fälschlich an, die Seiten-Kennung wäre Teil des Gewichtsfeld-Accessibility-Identifiers (`"Satz 1 L, Gewicht"`) – tatsächlich ist der Identifier wie bei jedem Satz rein `set.order`-basiert (`"Satz 0 Gewicht"`), die Seiten-Kennung steckt nur im davon unabhängigen, gesprochenen Label. Eine reine Existenzprüfung auf diesem falschen String hätte selbst eine komplett kaputte Seiten-Zuordnung nicht erkannt. Behoben durch einen neuen expliziten `.accessibilityIdentifier` auf der Kennung-Textansicht selbst (`"Satz N Kennung"`) und eine direkte Prüfung von deren gesprochenem Label (`"Satz 1 L, Links"` / `"Satz 1 R, Rechts"`) – der Test prüft jetzt tatsächlich die Alternierung, nicht nur irgendeine Satz-Existenz.
  - Unabhängiger Review-Pass: fand 5 echte Probleme, alle behoben:
    - **Fund 1**: `TrainingPlanDuplication` übernahm `isUnilateral` beim Duplizieren eines Plans nicht – eine duplizierte Übung wurde stillschweigend wieder beidarmig. Behoben durch Ergänzen des Felds in der kopierten `PlanExercise`-Konstruktion.
    - **Fund 2**: `PlanExportImport` exportierte/importierte `isUnilateral` nicht – ein exportierter und re-importierter Plan verlor die Einstellung. Behoben inkl. rückwärtskompatiblem Decoding (bereits vorher exportierte Dateien ohne das Feld werden weiterhin als beidarmig gelesen statt den ganzen Import scheitern zu lassen).
    - **Fund 3**: `addDropset` (Dropsatz während eines laufenden Trainings hinzufügen) setzte `side` gar nicht, wodurch ein Dropsatz auf einer einarmigen Übung keiner Seite zugeordnet war. Behoben durch Übernahme der Seite vom vorherigen Satz.
    - **Fund 4**: VoiceOver hätte bei der Kennung nur den Buchstaben ("L"/"R") vorgelesen statt des ausgeschriebenen Worts. Behoben durch eine eigene, sprechende `kennungDescription`, die Kennung und ausgeschriebene Seite ("Links"/"Rechts") kombiniert.
    - **Fund 5 (Drift-Risiko)**: `WorkoutSessionBuilder` (echte Sätze) und `PlanStartView` (Vorschau) berechneten die L/R-Alternierung ursprünglich unabhängig voneinander in zwei separaten Implementierungen – ein künftiger Änderungsfehler in nur einer der beiden hätte Vorschau und tatsächliche Session leise auseinanderlaufen lassen können. Behoben durch die neue geteilte `UnilateralSetPlan`-Hilfsfunktion (s. o.), die von beiden Stellen genutzt wird.

- **Trainingsansicht neu: Gesamt-/Übungsansicht-Umschalter + inline Satzzeilen + manueller Start + transparente Vorwerte** (zwei Backlog-Punkte zusammen umgesetzt, da dieselben Dateien betroffen sind). *(2026-08-14)*
  - Gebaut: `WorkoutSessionView` komplett neu – Gesamt-/Übungsansicht-Umschalter (Segmented Picker in der Toolbar, nur sichtbar ab 2 Übungen) mit Animation und Vor/Zurück-Navigation zwischen Übungen in der Übungsansicht; neue inline Satzzeile (`SetRow`) ersetzt das bisherige Antippen → `SetEditView`-Sheet komplett (Datei entfernt, die Ausrüstungs-Notiz bleibt über `PlanExerciseEditView` erreichbar): links die Satz-Kennung (A für Aufwärmsatz, D für Dropsatz, fortlaufende Nummer nur für normale Sätze – `SetKennung`), dann Gewicht/Wdh./Notiz direkt editierbar. Ghost-Werte-Mechanik: `SetEntry` bekommt `suggestedWeight`/`suggestedReps` (nur Anzeige-Hinweis über den nativ gedimmten TextField-Placeholder); `WorkoutSessionBuilder`/`WorkoutRepetition` befüllen bei normalen/wiederholten Sätzen nur noch diese Ghost-Felder, echte `weight`/`reps` starten leer; Eintragen der Wdh. füllt das Gewicht automatisch aus dem Ghost-Wert, sofern das Feld noch nie angefasst wurde. Satz gilt automatisch als erledigt sobald Gewicht UND Wdh. real gesetzt sind (`SetCompletion`), der bestehende manuelle Swipe-Toggle bleibt als Override erhalten. Manueller Start: neue `PlanStartView`-Vorschau (Plan antippen bzw. "Wiederholen" → Gesamtansicht ohne aktive Session → expliziter "Training starten"-Button) – die Vorschau berechnet sich rein aus bereits persistierten Daten (`plan.exercises` bzw. der Quell-Session eigene abgeschlossene Sätze), `WorkoutSessionBuilder`/`WorkoutRepetition` laufen unverändert erst synchron in `start()` direkt vor dem Insert.
  - Nicht offensichtlicher Bug unterwegs gefunden und behoben: frisch gebaute, aber noch nicht in den ModelContext eingefügte `SetEntry`-Objekte mit bereits gesetzter `session:`-Beziehung, über mehrere SwiftUI-Render-Zyklen in `@State` gehalten (z. B. während die Vorschau einfach nur angezeigt wird), brachten die Bildschirmdarstellung zum Absturz (kein Crash-Log, nur ein stiller "Warning"-Platzhalter – gefunden durch Extrahieren von Frames aus der XCUITest-Bildschirmaufnahme und Bisektion von `WorkoutSessionBuilder.build`). Deshalb baut die Vorschau bewusst keine echten `SetEntry`-Objekte vorab.
  - Getestet: neue Unit-Tests für `SetKennung` (A/D nie nummeriert, auch bei mehreren; normale Sätze fortlaufend), `SetCompletion`, `EffectiveSetValues` (reale vor Ghost-Werten, 0 wenn keins existiert), `WorkoutSessionBuilderTests`/`WorkoutRepetitionTests` auf Ghost-Verhalten umgestellt. Alle bestehenden UI-Tests, die über Zeilen-Tap → Sheet liefen, auf die neue Inline-Bedienung umgeschrieben (Felder wie `"Satz 0 Gewicht"`/`"Satz 0 Wdh"` statt Sheet-Felder); neuer `swipeHittableElement`-Test-Helfer (Pendant zu `tapHittableButton`) nötig, weil mehrere unabhängig interaktive Felder pro Zeile Mehrdeutigkeiten beim Swipen erzeugen. 6 neue XCUITests: manueller Start zeigt Vorschau vor Session-Erstellung, Zurück-Navigation hinterlässt keine Session, Ghost-Gewicht als Platzhalter + Autofill bei Wdh.-Eingabe, Gesamt-/Übungsansicht-Umschalten mit Navigation, sowie 2 Regressionstests für die beiden vom Review gefundenen Bugs (siehe unten). Gesamt 275 Unit-Tests + 35 UI-Tests grün (Suite lief wegen des 10-Minuten-Limits pro Testlauf in mehreren gezielten Teilläufen, keine offenen/unbestätigten Tests).
  - Unabhängiger Review-Pass: bestätigte den Ghost-Werte-Mechanismus, die `SetKennung`-Regeln, die Spurlosigkeit des manuellen Starts, das (unveränderte) Nicht-Reindizieren beim Löschen und die CloudKit-Konformität der neuen Felder, fand aber 5 echte Probleme, alle behoben:
    - **Hoch, mit temporärem Test empirisch verifiziert**: die manuelle "Erledigt"-Übersteuerung per Swipe war nicht dauerhaft – jede spätere Gewicht-/Wdh.-Änderung an derselben Zeile hat `isCompleted` stillschweigend wieder aus der Auto-Regel neu berechnet und die manuelle Entscheidung verworfen (z. B. ein bewusst erledigt markierter 0-kg-Bodyweight-Satz wurde durch eine reine Wdh.-Korrektur wieder als "nicht erledigt" markiert und wäre beim Beenden gelöscht worden). Behoben durch neues `SetEntry.isCompletionManual`-Flag, das die Auto-Regel nach einem manuellen Swipe für diese Zeile dauerhaft außer Kraft setzt.
    - **Mittel-hoch**: das Ghost-zu-real-Autofill des Gewichts prüfte `set.weight == 0`, was ein bewusst eingetragenes "0" (echtes Bodyweight-Training) nicht von einem noch unberührten Feld unterscheiden konnte – ein späteres Eintragen der Wdh. hätte die bewusste "0" stillschweigend mit dem alten Ghost-Gewicht überschrieben. Behoben durch ein lokales `weightManuallyEdited`-Flag in `SetRow`, das jede Berührung des Feldes verfolgt statt nur den resultierenden Wert.
    - **Mittel**: die Satz-Kennung war für VoiceOver unsichtbar (`.accessibilityHidden(true)`) und die übrigen Feld-Labels sprachen die interne, session-weite `set.order` statt der sichtbaren Kennung – Regression gegenüber dem direkt vorherigen Design-Polishing-Ticket, das genau diese Zeile für VoiceOver nachgerüstet hatte. Behoben: Kennung wieder sichtbar mit eigenem Label ("Aufwärmsatz"/"Dropsatz"/"Satz N"), `.accessibilityIdentifier` (stabil, `set.order`-basiert, für Tests) von `.accessibilityLabel` (sprechend, Kennung-basiert) getrennt.
    - **Mittel, Dynamic-Type-Risiko**: feste `.frame(width:)` auf den beiden editierbaren Zahlenfeldern widersprachen der im Design-Polishing-Ticket dokumentierten Codebase-weiten Nulltoleranz für genau dieses Muster. Auf `.frame(minWidth:)` umgestellt (Textabschneiden bei großer Schrift verhindert, ohne die Zeile bei normaler Schrift zu sprengen).
    - **Niedrig-mittel**: `focusedExerciseIndex` konnte veraltet/ungültig werden, wenn man in der Übungsansicht vor-/zurücknavigiert, zur Gesamtansicht wechselt, dort die Sätze der fokussierten Übung löscht und zurückwechselt – zeigte dann "Keine Übung" trotz vorhandener gültiger Übungen, weil die Korrektur nur per `onChange` lief, der nur bei gemounteter Übungsansicht feuerte. Behoben durch einen immer korrekten `safeFocusedExerciseIndex` statt reaktiver Korrektur.
    - Beim Beheben von Fund 3 zusätzlich eine eigene Ursache gefunden: ein `.accessibilityIdentifier(...)` auf der äußeren Satzzeile hatte die eigenen, spezifischeren Identifier der einzelnen Felder darunter überschrieben (SwiftUI vererbt einen auf einem zusammengesetzten Kind gesetzten Identifier an dessen Nachfahren-Elemente) – der Zeilen-Identifier wurde entfernt, das Gewichtsfeld dient seitdem als Zeilen-Adresse für Swipe-Aktionen in Tests.

- **Entwicklermodus mit In-App-Feedback** – 5×-Tap auf die Versionsnummer in den Einstellungen öffnet eine Passwort-Abfrage; im aktivierten Entwicklermodus gibt es eine eigenständige To-Do-Liste und einen 👎-Feedback-Button überall in der App. *(2026-08-14)*
  - Gebaut: `DeveloperUnlockSheet` (SecureField, Passwort `Isg#45krusgL.` als SHA-256-Hash im Code hinterlegt statt Klartext, prüft/togglet `isDeveloperModeActive` via `@AppStorage`), `DeveloperModeView` (To-Do-Sektionen offen/in Arbeit/erledigt, Feedback-Liste mit Prioritäts-Chips + Offen/Erledigt-Umschalter, "Entwicklermodus beenden"), `DeveloperFeedbackButton`/`FeedbackSubmitSheet` + `.developerFeedbackOverlay(...)`-ViewModifier, eingebunden auf allen 4 Tab-Wurzeln (Training – aktives Workout und Plan-Auswahl getrennt instrumentiert –, Pläne, Statistik, Einstellungen). Neue Models `FeedbackEntry`/`DevTodoItem`, Enum `FeedbackPriority` (in `Enums.swift`). Business-Logik in eigenen ViewModels: `DeveloperPasswordCheck`, `InProgressStore` (UserDefaults-Wrapper mit injizierbarem `UserDefaults` für Testisolation), `FeedbackFiltering`, `FeedbackPriorityDisplay`. 1:1-Vorbild war die bestehende Umsetzung in Lumio/Sunwake, bewusst an GymTracks CloudKit-Regeln angepasst: `priority` als `priorityRawValue: String` mit Computed Property statt direktem Codable-Enum wie im Original (das dagegen verstößt), alle gespeicherten Properties mit Default-Werten.
  - Getestet: Unit-Tests für `DeveloperPasswordCheck` (richtig, falsch, leer, Whitespace, Groß-/Kleinschreibung, fast richtiges Passwort ohne Schlusspunkt), `InProgressStore` (inkl. `allIDs()` für reaktive UI), `FeedbackFiltering` (alle Filter-Kombinationen), `FeedbackPriorityDisplay` (jeder Fall hat Label/Emoji, keine Duplikate). 4 neue XCUITests: Entsperren mit richtigem Passwort, falsches Passwort zeigt Fehler und lässt Dev-Mode aus, Feedback über 👎 senden und in der Liste wiederfinden, To-Do anlegen und erledigt markieren. Gesamt 257 Unit-Tests + 29 UI-Tests grün – die volle Suite läuft in dieser Umgebung länger als die verfügbaren 10 Minuten pro Testlauf, deshalb über einen kompletten Lauf (Unit-Ziel vollständig, UI-Ziel bis auf den letzten Test) plus gezielte Teilläufe für die verbleibenden Tests abgesichert, keine offenen/unbestätigten Tests.
  - Unabhängiger Review-Pass: fand einen kritischen Bug – `FeedbackEntry`/`DevTodoItem` fehlten in `PersistenceController.schema`, wodurch jedes Speichern eines Feedback-Eintrags oder To-Dos von `try?` still verschluckt wurde und beim nächsten Öffnen wieder leer war (die zwei dafür gebauten UI-Tests schlugen entsprechend fehl). Behoben durch Ergänzen beider Typen im Schema. Zusätzlich 4 kleinere, allesamt unverändert aus der Lumio-Vorlage übernommene Funde behoben: "Testen"/"Pause" aktualisierte die Sektion (To-Do/"In Arbeit") nicht reaktiv, da `InProgressStore` rohes UserDefaults liest statt SwiftUI-beobachtbaren State – behoben durch `@State`/`@Binding<Set<UUID>>` in `DeveloperModeView`/`DevTodoRow` statt direkter Store-Reads pro Zeile; hartcodierte `Color(red: 0.6, green: 0, blue: 0)` für "Dringend" verletzte die Dark-Mode-Vorgabe (verstößt gegen das Zero-`Color(red:)`-Prinzip aus dem Design-Polishing-Ticket) – durch eine dynamische `UIColor`-Variante ersetzt; Erledigt-Toggle in `DevTodoRow` speicherte nicht explizit; `InProgressStore`-Einträge blieben beim Löschen eines To-Dos verwaist.

- **Schnelles Erledigt-Markieren direkt in der Satzliste** – aktuell nur über das Bearbeiten-Sheet erreichbar. Reduziert das Risiko, dass "Entfernen & Beenden" beim Workout-Beenden versehentlich einen Satz löscht, den man ausgefüllt aber vergessen hat abzuhaken. *(2026-08-14)*
  - Gebaut: neue führende Swipe-Aktion ("Erledigt"/"Nicht erledigt" je nach aktuellem Status, grün/grau eingefärbt) auf jeder Satzzeile in `WorkoutSessionView`, ruft direkt `toggleCompleted(_:)` auf (`set.isCompleted.toggle(); try? modelContext.save()`) statt das Bearbeiten-Sheet zu öffnen. Bewusst als eigenständiger `.swipeActions(edge: .leading)`-Block (nicht als weiterer `Button` im bestehenden `HStack` der Zeile) – Swipe-Aktionen sind strukturell UIKit-Zeilenaktionen (`UIContextualAction`), keine gemeinsam gelayouteten Geschwister-Buttons, betreffen also nicht dieselbe Mehrfach-Tap-Bugklasse, die beim Dropsets-Ticket gefunden wurde. Koexistiert konfliktfrei mit der bereits bestehenden `.onDelete`-Swipe-Aktion am rechten Rand derselben Zeile (Muster analog zu `GymListView`/`PlanListView`, die ebenfalls beide Kanten gleichzeitig nutzen).
  - Getestet: keine neue Unit-Test nötig (`toggleCompleted` ist eine reine 2-Zeilen-Zustandsänderung, `WorkoutCompletion.incompleteSets` liest ohnehin nur `isCompleted` selbst und unterscheidet nicht nach Änderungsweg – bereits bestehend abgedeckt). 2 neue XCUITests: `testSwipingASetTogglesErledigtWithoutOpeningTheEditSheet` (Swipe → "Erledigt" antippen → Bearbeiten-Sheet öffnet sich NICHT, Zeilen-Label wechselt korrekt zu "erledigt"; Rückweg über "Nicht erledigt" ebenso geprüft) und `testEndingWorkoutAfterMarkingAllSetsErledigtViaSwipeSkipsPrompt` (verkettet die eigentliche Ticket-Motivation Ende-zu-Ende: alle Sätze per Swipe statt per Sheet abgehakt → "Beenden" löst NICHT den "nicht ausgefüllte Sätze entfernen"-Alert aus). Gesamt 232 Unit-Tests + 25 UI-Tests grün.
  - Unabhängiger Review-Pass: APPROVE ("Ship as-is"), keine blockierenden Findings. Reviewer hat eigenständig nachgelesen, dass `.sheet(item: $setToEdit)` ausschließlich von `setToEdit` getriggert wird und `toggleCompleted` dieses State nie berührt – ein versehentliches Öffnen des Bearbeiten-Sheets durch die neue Swipe-Aktion ist damit strukturell ausgeschlossen, nicht nur durch Timing. Hat außerdem `WorkoutCompletion.swift` direkt gelesen und bestätigt, dass die Fertig-Prüfung rein feldbasiert ist (kein pfadabhängiges Verhalten zwischen Sheet-Toggle und Swipe möglich). Einzigen Fund (Ende-zu-Ende-Test für die eigentliche Ticket-Motivation fehlte, als "gering riskant, aber ein echter Fund" eingestuft) noch am selben Tag mit `testEndingWorkoutAfterMarkingAllSetsErledigtViaSwipeSkipsPrompt` geschlossen statt vertagt.

- **Durchgängiges Design-Polishing** – SF Symbols, Light/Dark Mode, Dynamic Type & VoiceOver-Grundlagen über alle bis dahin gebauten Screens. *(2026-08-14)*
  - Gebaut: systematische Zeile-für-Zeile-Durchsicht aller 14 View-Dateien unter `GymTrack/Views` gegen vier Kriterien. Ergebnis: die Codebase war bereits weitgehend konform (kein hartcodiertes `Color(red:)`/`UIColor(...)` irgendwo – ausschließlich adaptive System-Farben wie `.primary`/`.secondary`/`.tint`/`.blue`/`.green`/`.orange`/`.purple`, die automatisch auf Light/Dark Mode reagieren; SF Symbols konsistent verwendet, keine Kollisionen – "plus" einheitlich für jede "Hinzufügen"-Aktion, Icons in Empty-States passend zum jeweiligen Konzept; kein `.frame(width:)`/`.lineLimit()` irgendwo, das bei großer Dynamic-Type-Schrift Text abschneiden könnte). Zwei konkrete VoiceOver-Lücken gefunden und behoben: `StatRow` (Statistik-Tab, "Übersicht" + "Häufigste Übungen") hatte im Gegensatz zur benachbarten `PersonalRecordRow` in derselben Datei kein `.accessibilityElement(children: .combine)` – VoiceOver hätte Titel und Wert als zwei getrennte Stopps statt einer kombinierten Ansage gelesen; ergänzt. `SetRow` (aktives Training, die mit Abstand am häufigsten genutzte Zeile der App) verließ sich rein auf die automatisch aus SF-Symbol-Systembeschreibungen zusammengesetzte Ansage (z. B. "Häkchen, Kreis, gefüllt" statt klar "erledigt"/"nicht erledigt") – durch ein explizites `.accessibilityLabel`, das Satztyp (Aufwärm-/Drop-/normaler Satz), Gewicht×Wdh. und Erledigt-Status klar benennt, ersetzt.
  - Getestet: keine neuen Unit-Tests (reine View-/Accessibility-Änderungen ohne neue Business-Logik). Beide Änderungen gezielt gegen die einzigen Tests geprüft, die betroffene Zeilen berühren (`testCompletingAWorkoutShowsStatistics`, `testCompletingAWorkingSetShowsAPersonalRecordEstimate`) – beide blieben grün, da `app.buttons`/`app.staticTexts`-Lookups über Accessibility-*Identifier* laufen (bei `SetRow` z. B. "Satz 0"), nicht über den geänderten *Label*-Text. Gesamt 232 Unit-Tests + 23 UI-Tests grün (ein voller Suite-Lauf zeigte währenddessen einmalig 8 thematisch unzusammenhängende Fehlschläge gleichzeitig – siehe neue Notiz beim "Flaky UI-Test stabilisieren"-Punkt unter "Offen"; isoliert und im nächsten vollen Lauf sofort wieder komplett grün, kein Zusammenhang mit diesem Ticket).
  - Unabhängiger Review-Pass: zwei Runden. Erste Runde bestätigte die Diagnose (Codebase weitgehend konform) und den `StatRow`-Fix als sicher, fand aber die `SetRow`-Lücke als übersehenen, gleichwertigen Fall auf einem noch häufiger genutzten Screen – direkt in dieses Ticket aufgenommen statt vertagt, da CLAUDE.md Barrierefreiheit "von Anfang an" verlangt und diese Zeile die meistgenutzte der App ist. Als bewusst nicht behobene, niedrigpriorisierte Randfunde notiert: das 🔗-Emoji in den Superset-Beschriftungen (Plan-Editor und aktives Training) ist die einzige Nicht-SF-Symbol-Ikonografie der View-Schicht und ignoriert `.foregroundStyle(.tint)` (Apple-Color-Emoji tragen eigene feste Farben) – rein kosmetisch, keine Funktionseinbuße, bewusst belassen statt einer Layout-Änderung an einer bereits fertigen Funktion; `PlanRow`/`PlanTemplatePickerView`s Zeilen haben als einzige mehrzeiligen Listenzeilen kein explizites `.accessibilityElement(children: .combine)`, vom Reviewer aber nicht als bestätigter Fehler eingestuft (Button/NavigationLink kombinieren ihre Label-Inhalte laut etabliertem, im Rest der Codebase bereits mehrfach beobachtetem Verhalten standardmäßig selbst) – als spätere VoiceOver-Stichprobe notiert statt jetzt ohne bestätigten Fehler geändert.

- **Supersets** – mehrere Übungen als Gruppe direkt hintereinander loggen, visuell klar gruppiert. *(2026-08-14)*
  - Gebaut: `supersetGroupID: UUID? = nil` auf `PlanExercise` (neu) und `SetEntry` (bereits seit dem Datenmodell-Grundgerüst vorhanden, aber bis jetzt nirgends befüllt) – eine geteilte, frisch generierte ID ist das einzige, was Gruppen-Mitglieder verbindet, keine eigene "Gruppe"-Entity. `SupersetGrouping` (reine Business-Logik): `group` weist einer Auswahl von ≥2 Übungen eine neue geteilte ID zu und räumt dabei auch frühere Gruppenpartner auf, die nicht Teil der neuen Auswahl sind (sonst bliebe z. B. bei A+B gruppiert → nur A+C neu gruppiert ein verwaistes B mit einer ID zurück, die niemand mehr teilt); `leave` entfernt eine einzelne Übung aus ihrer Gruppe und löst die Gruppe komplett auf, falls dadurch weniger als 2 Mitglieder übrig blieben (ein "Superset" aus einer Übung ist nicht sinnvoll); `partnerNames` für die Anzeige. `WorkoutSessionBuilder` überträgt die `supersetGroupID` jedes `PlanExercise` auf jeden daraus erzeugten `SetEntry`. UI: neuer "Gruppieren"/"Fertig"-Umschalter im Plan-Editor (Mehrfachauswahl-Modus für Übungen, deaktiviert solange der native Bearbeiten-Modus aktiv ist und umgekehrt, um nicht zwei gleichzeitig aktive, sich widersprechende Zeilen-Tap-Verhalten zu haben), Swipe-Aktion "Superset auflösen", 🔗-Beschriftung sowohl im Plan-Editor als auch während eines laufenden Trainings.
  - Getestet: 13 Unit-Tests für `SupersetGrouping` (Gruppieren von 2/3 Übungen, <2 ist No-op, Neugruppieren derselben vollständigen Auswahl vereint sie in einer neuen ID, Neugruppieren einer TEILMENGE einer bestehenden 2er/3er-Gruppe räumt zurückgelassene Mitglieder korrekt auf – entweder komplett aufgelöst oder untereinander weiter gruppiert je nach verbleibender Anzahl, `ungroup`, `leave` bei 2/3 Mitgliedern und bei bereits ungruppierter Übung, `partnerNames` inkl. Ausschluss der Übung selbst) + 2 neue `WorkoutSessionBuilder`-Tests (Sätze erben die `supersetGroupID` ihres `PlanExercise`, Sätze einer eigenständigen Übung bleiben `nil`) + 2 neue XCUITests (`testGroupingTwoExercisesShowsASupersetIndicatorOnBoth`, `testStartingAWorkoutFromAPlanWithGroupedExercisesShowsTheSupersetCaption`). Gesamt 232 Unit-Tests + 23 UI-Tests grün.
  - Die beiden neuen UI-Tests brauchten mehrere Anläufe, bis sie zuverlässig liefen – alles reine Test-Infrastruktur-Probleme, kein App-Bug: (1) der "Gruppieren"-Button teilte sich zunächst `.primaryAction` mit `EditButton()`, was auf manchen Simulator-Breiten zu Toolbar-Überlauf und einem ungültigen Tap-Hit-Point führte; (2) als Fix probeweise auf `.bottomBar` verschoben, was sich aber mit der TabView-eigenen Tableiste visuell überlappte – ein synthetischer Tap auf "Gruppieren" landete stattdessen auf dem "Statistik"-Tab-Button darunter (per Diagnose-Dump bestätigt: die App war danach tatsächlich auf dem Statistik-Tab); endgültig auf `.topBarLeading` gelöst. (3) Das Hinzufügen der zweiten Übung im Picker-Sheet race'te gegen die Sheet-Dismiss-Animation und gegen SwiftUIs `@Query`-Refresh, wodurch der Test teils in ein noch offenes Sheet zurücktappte oder einen (mangels aktualisierter Übungsanzahl) noch deaktivierten "Gruppieren"-Button antippte; behoben durch explizites Warten auf das Verschwinden des Picker-Sheets sowie auf das Wiedererscheinen der neu hinzugefügten Übung im Editor selbst. (4) "Rudern vorgebeugt" lag im Picker unterhalb des sichtbaren Bereichs (Muskelgruppe "Rücken") – automatisches Scrollen von `tap()` löste dort denselben ungültigen-Hit-Point-Effekt wie bei (1) aus; behoben durch Filtern über das Picker-eigene Suchfeld statt Scrollen. (5) Im Gruppierungs-Modus lieferte `app.buttons[name]` zwei Accessibility-Elemente mit identischem Identifier (das echte, tappbare Zeilen-Element und ein nicht-tappbares Begleit-Element der List/Form-Infrastruktur); ein neuer `tapHittableButton`-Test-Helfer wählt gezielt das tatsächlich tappbare Element und schlägt hart fehl, falls mehr als eines gleichzeitig tappbar wäre (verhindert, dass eine echte künftige Mehrdeutigkeit – z. B. dieselbe Übung zweimal im selben Plan – still das falsche Element treffen könnte).
  - Unabhängiger Review-Pass: Erster Durchlauf fand einen echten Bug (nicht nur Randfälle) – noch am selben Tag behoben, danach kompletter Test-Lauf erneut grün:
    - **Kritisch behoben**: Löschen einer Übung aus dem Plan-Editor rief `SupersetGrouping.leave` nicht auf, bevor `modelContext.delete` griff – der verbleibende Gruppenpartner behielt eine `supersetGroupID`, die niemand mehr teilte, und zeigte weiterhin die "Superset auflösen"-Swipe-Aktion an, obwohl visuell keine Gruppierung mehr sichtbar war. Behoben: `deletePlanExercises` ruft jetzt `SupersetGrouping.leave` für jede gelöschte Übung auf, bevor sie entfernt wird; derselbe Aufräum-Mechanismus wurde direkt auch in `SupersetGrouping.group` selbst eingebaut (siehe oben, Teilmengen-Neugruppierung), da beide Stellen denselben verwaisten-ID-Fehlerklasse teilten.
    - **Mittel behoben**: Gruppierungs-Modus und der native Bearbeiten-Modus (`EditButton`) konnten gleichzeitig aktiv sein – beide Buttons zeigten dann verwirrenderweise gleichzeitig "Fertig" und es war unklar, ob ein Zeilen-Tap dann sortiert/löscht oder zur Gruppierung auswählt. Behoben: beide Modi schließen sich jetzt gegenseitig aus (`.disabled`).
    - **Niedrig behoben**: VoiceOver-Auswahlstatus im Gruppierungs-Modus war rein visuell (Farbe/Symbol) ohne Accessibility-Trait – `.accessibilityAddTraits(.isSelected)` ergänzt. Neu hinzugefügte Sätze während eines laufenden Trainings (Aufwärm-/Drop-/normaler Satz) übernahmen bislang keine `supersetGroupID` vom Rest der Übung – ergänzt, aktuell folgenlos (die Beschriftung liest nur den ersten Satz), aber als Konsistenz-Absicherung für künftigen Code sinnvoll.
    - Als bewusst nicht behobene Randfälle notiert (kein Blocker): dieselbe Übung zweimal im selben Plan hinzuzufügen ist nirgends verhindert – falls das passiert und nur eine der beiden Instanzen gruppiert ist, gruppiert `WorkoutSessionView` deren Sätze trotzdem zusammen (bestehendes Verhalten, unabhängig von diesem Ticket) und die Superset-Beschriftung könnte dadurch falsch zugeordnet werden; `SupersetGrouping.ungroup` (Plural) wird aktuell nirgends im Produktivcode aufgerufen (nur `leave` für einzelne Übungen) – als kleine, gut getestete Hilfsfunktion bewusst belassen statt entfernt, deutet aber auf eine fehlende "Mehrere auf einmal entgruppieren"-Aktion hin.

- **Statistik: Personal Records / geschätztes 1RM** – gym-übergreifend vergleichbar via Umrechnungsfaktor. Nur `isCompleted == true`-Sätze zählen. Unit-Tests für die 1RM-Formel inkl. Edge Cases (0 Wiederholungen, keine Historie). *(2026-08-13)*
  - Gebaut: `OneRepMax.estimate(weight:reps:)` (Epley-Formel; 1 Wiederholung entspricht exakt dem Gewicht ohne Hochrechnung; 0/negatives Gewicht oder Wdh. ergibt 0 statt Crash/NaN). `PersonalRecordFinder.bestPerExercise` – rein lesende Berechnung direkt aus der Satz-Historie (wie `SessionStatistics`/`ExerciseFrequency`/`TrainingVolume`, kein Rückschreiben ins bereits seit dem allerersten Datenmodell-Ticket bestehende, aber nie befüllte `PersonalRecord`-Model – bewusst so belassen, da eine zustandslose Neuberechnung genau demselben Muster wie alle anderen Statistik-Features folgt und es keinen sinnvollen Moment gibt, an dem ein PR "committed" werden müsste). Findet pro Übung den Satz mit dem höchsten, gym-umgerechneten geschätzten 1RM (`GymConversion`, inkl. Override pro Übung+Gym vor dem globalen Gym-Faktor). Aufwärm- und Dropsätze zählen bewusst nicht (analog zur bestehenden `SetSuggestion`-Konvention, dass sie keine echte Maximalleistung darstellen) – bewusst anders als beim "Trainingsvolumen"-Ticket, das Aufwärm-/Dropsätze bewusst mitzählt, da Volumen die gesamte geleistete Arbeit misst, PRs aber die Spitzenleistung. Neue Sektion "Bestleistungen (geschätztes 1RM)" im Statistik-Tab, bewusst ohne Kürzung (anders als "Häufigste Übungen" mit Top 5), da durch den Übungs-Katalog selbst natürlich begrenzt.
  - Getestet: 7 Unit-Tests für `OneRepMax` (Epley-Formel, 1-Wdh.-Sonderfall, 0 Wdh./Gewicht, negative Wdh. crasht nicht, höhere Wdh. → höherer Wert, sehr hohe Wdh. bleibt endlich) + 14 Unit-Tests für `PersonalRecordFinder` (leere Historie, wählt höchsten statt jüngsten 1RM, unerledigte/Aufwärm-/Dropsätze ausgeschlossen selbst bei höherem Rohgewicht, nicht-zählbare Sessions ausgeschlossen, Übung ohne Referenz übersprungen, mehrere Übungen unabhängig, alphabetische Sortierung, gym-übergreifender Vergleich inkl. Override-Vorrang vor globalem Gym-Faktor, 0-Wiederholungen-Satz erzeugt keinen Geister-Rekord, deterministischer Unentschieden-Tiebreak nach Datum statt Array-Reihenfolge) + 1 neuer XCUITest (Arbeitssatz mit 100 kg × 10 Wdh. abschließen → Statistik zeigt "≈ 133 kg"). Gesamt 217 Unit-Tests + 21 UI-Tests grün.
  - Unabhängiger Review-Pass: APPROVE. Reviewer hat die komplette Mathematik von Hand nachgerechnet (Epley-Formel, gym-übergreifende Umrechnung inkl. Override-Vorrang, Un­entschieden-Fall) statt sich auf die Tests zu verlassen, das verwaiste `PersonalRecord`-Model als unkritisch bestätigt (wird nirgends sonst im Code gelesen/geschrieben) und die Aufwärm-/Dropsatz-Ausschluss-Entscheidung explizit als bewusst-unterschiedlich vom Trainingsvolumen-Ticket bestätigt, nicht als Inkonsistenz. Einen nicht-blockierenden Randfall gefunden (Unentschieden bei exakt gleichem 1RM wurde nach unspezifizierter SwiftData-Array-Reihenfolge statt nach Datum aufgelöst, könnte zwischen App-Starts inkonsistent wirken) – noch am selben Tag behoben (deterministischer Tiebreak nach jüngstem Datum) und mit einem eigenen Regressionstest abgesichert.

- **Dropsets** – mehrere Sätze derselben Übung direkt hintereinander mit abnehmendem Gewicht ohne Pause, als zusammengehörige Einheit erkennbar. *(2026-08-13)*
  - Gebaut: `DropsetSuggestion.suggestedWeight(previousWeight:)` (20 % leichter als der Satz, von dem abgelegt wird; jeder weitere Dropsatz staffelt sich vom vorherigen Dropsatz weiter ab, nicht vom ursprünglichen Arbeitsgewicht). Neuer "Dropsatz"-Button neben "Aufwärmsatz"/"Satz hinzufügen"; `addDropset` hängt den neuen Satz einfach ans Ende der Sätze dieser Übung an (keine Umsortierung nötig, anders als bei Aufwärmsätzen, die vor die Arbeitssätze müssen). Lila Pfeil-Icon in der Satzliste für Dropsätze, analog zum orangenen Flammen-Icon für Aufwärmsätze. Bezieht das "Ausgangsgewicht" bewusst vom letzten NICHT-Aufwärmsatz (nicht einfach vom letzten Satz überhaupt) – sonst würde ein Dropsatz direkt nach einem Aufwärmsatz eine unsinnig niedrige Gewichtsempfehlung bekommen.
  - **Kritischer, vorbestehender Bug gefunden und behoben** (betraf auch den bereits fertigen "Aufwärmsatz"-Button, nicht nur den neuen Dropsatz-Code): Mehrere `Button`s im selben `HStack` einer Listenzeile ohne expliziten `.buttonStyle` verschmolzen ihre Tap-Flächen – ein Tippen auf IRGENDEINEN der Buttons löste ALLE Aktionen im HStack gleichzeitig aus, wodurch pro Tap mehrere Sätze statt einem angelegt wurden (mit den ursprünglich 2 Buttons "Aufwärmsatz"/"Satz hinzufügen": +2 statt +1 pro Tap; mit dem durch dieses Ticket hinzugefügten dritten Button: +3 statt +1). Die Ursache wurde erst nach ausführlicher Untersuchung gefunden (SwiftData-Doppel-Insert-Theorie, Dispatch-Verzögerung, `Task.sleep`, stabile View-Identität, explizites `fetch()` zum Leeren anstehender Änderungen und eine zufällig gleichzeitig gebootete zweideutige Simulator-Instanz wurden alle geprüft und als Ursache ausgeschlossen) – der eindeutige Beweis kam erst, als Buttons einzeln aus dem HStack entfernt wurden: mit genau einem Button im HStack erzeugte ein Tap exakt einen neuen Satz, mit zwei Buttons exakt zwei, mit drei exakt drei. Behoben durch `.buttonStyle(.borderless)` auf jedem der drei Buttons (analog zum bereits korrekt funktionierenden `.buttonStyle(.plain)` auf den Satz-Zeilen selbst). Der Bug wurde erst am Vortag dieses Tickets eingeführt (Aufwärmsatz-Button kam am 2026-08-12 in einem eigenen Ticket dazu, vorher gab es nur einen einzelnen Button ohne Geschwister) – nicht seit Projektbeginn vorhanden.
  - Getestet: 4 Unit-Tests für `DropsetSuggestion` (exakter 80%-Wert, Ergebnis immer leichter als Eingabe, 0-Gewicht crasht nicht, Staffelung vom vorherigen statt ursprünglichen Gewicht) + 2 neue Regressions-UI-Tests (`testAddingASetCreatesExactlyOneNewSetNotTwo`, `testAddingAWarmupSetCreatesExactlyOneNewSetNotTwo` – prüfen explizit, dass genau EIN neuer Satz pro Tap entsteht, kein Satz zu viel) + 1 neuer XCUITest für den Dropsatz-Flow selbst (Gewicht setzen → Dropsatz hinzufügen → korrekte 80%-Vorschlag → per Swipe löschen → Ausgangssatz-Gewicht unverändert). Gesamt 196 Unit-Tests + 20 UI-Tests grün.
  - Unabhängiger Review-Pass: APPROVE. Reviewer hat den Fix per Mutationsprobe direkt verifiziert (einzelnen `.buttonStyle(.borderless)` entfernt → Tests weiterhin grün, da nur EIN Button betroffen; alle drei entfernt → beide neuen Regressionstests schlagen exakt wie erwartet fehl → wiederhergestellt, Diff danach byte-identisch). Hat außerdem den gesamten `Views`-Ordner selbst nach demselben Muster (mehrere Buttons in einem HStack ohne `.buttonStyle` in einer Liste) durchsucht – keine weiteren betroffenen Stellen gefunden, der Bug war auf `WorkoutSessionView` isoliert. Eine Ungenauigkeit in der ursprünglichen Formulierung korrigiert (der Bug bestand erst seit dem Vortag, nicht seit Projektbeginn) und einen Randfall gefunden, der noch am selben Tag behoben wurde: `addDropset` bezog das Ausgangsgewicht ursprünglich vom letzten Satz unabhängig vom Typ statt vom letzten Nicht-Aufwärmsatz – behoben.

- **Import/Export von Plänen (JSON)** – Export über iOS Share Sheet, Import z. B. aus der Files-App, inkl. Validierung/Fehlermeldung bei beschädigter Datei. Unit-Tests fürs Parsing inkl. Edge Case "beschädigte Datei". *(2026-08-13)*
  - Gebaut: `PlanExport` (Codable Snapshot: Name, Notiz, Übungen referenziert per Name statt per Objekt/ID – analog zu `PlanTemplateApplication`, da IDs geräteübergreifend keine gemeinsame Bedeutung haben). `PlanExportImport.encode`/`decode` (JSON, jeder Decode-Fehler egal welcher Art wird als ein einziger `.corruptedFile`-Fehler nach außen gegeben statt roher `DecodingError`-Varianten), `makePlan` (matcht Einträge per Name gegen die echte Bibliothek, überspringt nicht gefundene statt abzustürzen, sortiert nach dem eigenen `order`-Feld der Einträge – nicht nach Array-Position – bevor lückenlos neu indiziert wird). UI: neue "Exportieren"-Swipe-Aktion (führende Kante, neben "Duplizieren") über `ShareLink` mit temporärer `.json`-Datei; dritter Toolbar-Button "Plan importieren" via `.fileImporter`, inkl. korrektem Security-Scoped-Resource-Handling und Fehler-Alert bei beschädigter/ungültiger Datei.
  - Getestet: 15 Unit-Tests für `PlanExportImport` (Export-Mapping inkl. Notiz/Gewicht/Notiz pro Übung, Export überspringt Einträge ohne Übungs-Referenz, Encode/Decode-Rundtrip inkl. `nil`-Werten und leerem Plan, drei verschiedene "beschädigte Datei"-Varianten – Datenmüll, leere Datei, valides JSON mit falscher Struktur/falschem Typ –, `makePlan`-Matching inkl. bewusst absichtlich falsch sortierter Test-Eingabe um eine naive Array-Reihenfolge-statt-`order`-Feld-Implementierung zu erkennen, vollständiger End-to-End-Rundtrip) + 1 neuer XCUITest, der bewusst NICHT die echte System-Freigabe/Dateiauswahl automatisiert (separate, in XCUITest notorisch unzuverlässige OS-Prozesse), sondern nur die Erreichbarkeit beider Einstiegspunkte prüft. Gesamt 192 Unit-Tests + 17 UI-Tests grün.
  - Unabhängiger Review-Pass: APPROVE, keine blockierenden Findings. Reviewer hat die Doppel-Insert-Bugklasse durch direkten Vergleich mit den bereits verifizierten `PlanTemplateApplication`/`TrainingPlanDuplication` bestätigt (identisches `TrainingPlan`/`PlanExercise`-Beziehungsmuster, kein neuer Test nötig) und den Order-Sortier-Test gezielt gegen eine naive Implementierung nachgerechnet. Zwei nicht-blockierende Randfälle notiert: ein sehr langer Plan-Name (>~250 Zeichen) lässt die temporäre Export-Datei mit `ENAMETOOLONG` scheitern, wodurch der Exportieren-Button ohne Erklärung verschwindet (`try?` schluckt den Fehler) – als künftige Design-Polishing-Notiz akzeptiert, kein Crash/Datenverlust; und die temporäre Export-Datei wird eager beim Rendern jeder Swipe-Aktion geschrieben statt erst bei tatsächlichem Antippen – für die Größenordnung dieser Solo-Nutzer-App harmlos, aber als spätere Optimierung notiert.

- **Letztes Training wiederholen** – ein vorheriges (abgeschlossenes) Training als Vorlage für eine neue Session übernehmen, auch wenn es nicht aus einem Plan gestartet wurde. *(2026-08-13)*
  - Gebaut: `WorkoutRepetition.build(repeating:gym:)` (reine Business-Logik, analog zu `WorkoutSessionBuilder.build(from: plan...)`, aber als Quelle eine vergangene `WorkoutSession` statt eines `TrainingPlan`) kopiert alle erledigten Sätze der Quell-Session (Gewicht/Wdh./Satztyp exakt wie geloggt, nicht über `SetSuggestion` neu hergeleitet – bewusste Entscheidung: die Quell-Session ist bereits die jüngste Historie für ihre eigenen (Übung,Gym)-Paare, ein Wiederherleiten würde nur denselben Wert mit mehr Code reproduzieren) als frische, unerledigte Sätze; nutzt das aktuell aktive Gym, nicht das Gym der Quell-Session. Neue Sektion "Wiederholen" im Training-Tab (oberhalb "Plan starten") zeigt einen Button für die zuletzt zählbare Session (`SessionStatistics.eligibleSessions`, jüngste zuerst), auch wenn noch gar keine Pläne existieren.
  - Getestet: 12 Unit-Tests für `WorkoutRepetition` (leere Quelle, Kopie von Gewicht/Wdh./Übung, Sätze starten immer unerledigt, unerledigte Quell-Sätze werden nicht kopiert, Satztyp bleibt erhalten, lückenlose Order trotz Lücken in der Quelle, Übung ohne Referenz wird übersprungen, Plan-Name wird übernommen inkl. `nil`-Fall ohne Plan, aktuelles statt Quell-Gym, kein Enddatum, jeder Satz zeigt korrekt auf die neue Session) + 1 neuer XCUITest (Training abschließen → "Letztes Training wiederholen" → landet direkt in einer neuen, unerledigten Session mit derselben Übung). Gesamt 177 Unit-Tests + 16 UI-Tests grün.
  - Unabhängiger Review-Pass: APPROVE, keine blockierenden Findings. Reviewer hat die aus früheren Tickets bekannte Doppel-Insert-Bugklasse erneut gezielt per eigenem Test-Probe empirisch geprüft (Relationship-Struktur von `WorkoutSession`/`SetEntry` strukturell identisch zu der bereits verifizierten `TrainingPlan`/`PlanExercise`) und **keinen** Bug gefunden; hat alle vier Kombinationen aus "Pläne vorhanden/leer" × "wiederholbare Session vorhanden/keine" im Training-Tab von Hand durchgespielt und keine Lücke gefunden (nie eine leere Liste ohne jeglichen Inhalt); und "Wiederholung einer Wiederholung" nachvollzogen (`build()` schaut nie über die unmittelbare Quelle hinaus, kein Ketten-Risiko). Nicht-blockierender Hinweis: das kopierte Gewicht läuft nicht durch `GymConversion`, falls sich das aktive Gym seit der Quell-Session geändert hat – konsistent mit dem bereits bestehenden Verhalten von `WorkoutSessionBuilder`, als mögliches künftiges Ticket notiert statt jetzt behoben.

- **Vorlagen-basierte Pläne** – mitgelieferte Split-Vorlagen (z. B. Push/Pull/Legs, Ganzkörper) als Startpunkt, danach frei anpassbar. *(2026-08-13)*
  - Gebaut: `PlanTemplateLibrary` (4 statische Vorlagen: Push/Pull/Legs/Ganzkörper, je 5 Übungen mit Ziel-Sätzen/-Wdh., referenziert Übungen bewusst nur per Name statt per `Exercise`-Objekt, da Vorlagen statische Daten ohne ModelContext-Bezug sind). `PlanTemplateApplication.makePlan` matcht jeden Vorlagen-Eintrag per Name gegen die tatsächliche Übungs-Bibliothek und baut daraus Plan+PlanExercises; nicht gefundene Einträge (umbenannt/gelöscht) werden übersprungen statt abzustürzen oder eine Dublette anzulegen, `order` bleibt dabei lückenlos. Neuer zweiter Toolbar-Button "Aus Vorlage erstellen" in der Plan-Liste (bewusst zusätzlich zum bestehenden "+", nicht als Ersatz, um die ~6 bestehenden UI-Tests für die Leerplan-Erstellung nicht anzufassen) öffnet einen Vorlagen-Picker; Auswahl erstellt den Plan direkt und springt in den Editor.
  - Getestet: 11 Unit-Tests für `PlanTemplateApplication` (Name-Übernahme, korrekte Sätze/Wdh. pro Eintrag, lückenlose Order, übersprungene Einträge crashen nicht und lassen Order lückenlos, leere Bibliothek/keine Treffer erzeugt einen validen leeren Plan, `PlanExercise.plan`-Referenz korrekt gesetzt) + 1 Konsistenz-Test, der jede in den Vorlagen referenzierte Übung gegen `ExerciseSeeder.defaultExercises` prüft (verhindert stillen "Vorlagen-Verfall" bei künftigen Bibliotheks-Änderungen) + 1 neuer XCUITest (Vorlage "Push" wählen → Planname und zwei der fünf Übungen direkt im Editor sichtbar). Gesamt 165 Unit-Tests + 15 UI-Tests grün.
  - Unabhängiger Review-Pass: APPROVE, keine blockierenden Findings. Reviewer hat die aus früheren Tickets bekannte Doppel-Insert-Bugklasse (SwiftData verknüpft ein neues Kind-Objekt schon über den `plan:`-Init-Parameter, bevor es explizit inserted wird) gezielt empirisch mit einem eigenen Test-Probe gegen einen echten In-Memory-`ModelContext` geprüft und **keinen** Bug gefunden – exakt dasselbe, bereits produktiv genutzte Muster wie in `TrainingPlanDuplication`. Ein SF-Symbol-Kollisionshinweis (neuer Toolbar-Button nutzte ein optisch fast identisches Icon zu einem bereits anders belegten) wurde direkt behoben (`square.stack` statt `list.bullet.rectangle.portrait`), danach erneuter kompletter Test-Lauf grün. Reviewer merkte zusätzlich an, dass ein Nutzer, der versehentlich *alle* Übungen einer Vorlage löscht, einen stillen leeren Plan ohne Hinweis bekäme – als bewusst akzeptierte, sehr unwahrscheinliche Lücke notiert, nicht behoben.
  - Reviewer-Beobachtung (kein Fund an diesem Ticket, aber neu notiert): `testGymConversionFactorPersistsAfterEditing` flackerte einmal bei einem vollen Suite-Lauf (Ressourcen-Konkurrenz vermutet, 3× isoliert sofort danach grün) und lief im abschließenden Lauf dieses Tickets ebenfalls sauber durch – als Beobachtung für spätere Stabilisierung unter "Offen" ergänzt.

- **Gym-Umrechnungsfaktor** – optionaler Umrechnungsfaktor zwischen Gyms (pro Übung oder global einstellbar), damit Statistiken/PRs gym-übergreifend auf ein Referenz-Gym umgerechnet vergleichbar sind. Unit-Tests für die Umrechnung, inkl. Edge Case "Gym ohne Historie". *(2026-08-12)*
  - Gebaut (bewusst nur die Grundlage, analog zu den früheren "Datenmodell-Erweiterung"-Tickets – die eigentliche gym-übergreifende Statistik ist das noch offene PR/1RM-Ticket): `Gym.weightConversionFactor` (global, Default 1.0 = keine Umrechnung) + `ExerciseGymNote.conversionFactor` (optionaler Override pro Übung+Gym, `nil` = erbt den globalen Gym-Wert) – letzteres nutzt bewusst die bestehende (Übung,Gym)-Entity statt einer dritten neuen. `GymConversion` (reine Business-Logik, kein SwiftData-Bezug) rechnet ein Gewicht von einem Faktor auf einen anderen um; nicht-positive Faktoren (0 oder negativ) werden sowohl beim Speichern als auch in der Umrechnung selbst wie "1.0/kein Override" behandelt statt eine Division durch 0 oder ein fälschlich auf 0 umgerechnetes Gewicht zu riskieren. UI: Umrechnungsfaktor-Feld in den Gym-Einstellungen (global) und im Plan-Editor bei der Ausrüstungs-Notiz (Override pro Übung) – bewusst nicht zusätzlich im Satz-Bearbeiten-Sheet, da eine einmalige Kalibrierungs-Einstellung ist, keine pro-Satz veränderliche Notiz.
  - Getestet: 10 Unit-Tests für `GymConversion` (Override vs. globaler Faktor, unveränderte Gewichte bei Faktor 1, korrekte Skalierung, Symmetrie der Umrechnung, 0/negativer Faktor auf beiden Seiten wird wie "keine Umrechnung" behandelt statt Division durch 0 oder stillem Nullen des Gewichts, Gewicht 0 bleibt 0, explizites Edge Case "Gym ohne Historie" – Umrechnung hängt nie von der Satz-Historie ab) + 3 neue Model-Tests (Defaults/Settability für beide neuen Felder) + 1 neuer XCUITest (Umrechnungsfaktor setzen → Gym erneut öffnen → Wert persistiert). Gesamt 154 Unit-Tests + 14 UI-Tests grün.
  - **Bug im eigenen UI-Test gefunden und behoben** (kein Produktionsbug, aber eine echte XCUITest-Eigenheit): ein einzelner Tap auf das mit "1" vorbefüllte Feld positionierte den Cursor VOR statt NACH dem Text – Löschen per Zeichen-Anzahl löschte dadurch nichts, und der neue Wert wurde vor die "1" eingefügt ("0.81" statt "0.8"). Durch einen echten fehlgeschlagenen Testlauf entdeckt (nicht durch Review), behoben durch `doubleTap()` (selektiert den vorhandenen Text zuverlässiger) statt zeichenweisem Löschen, isoliert verifiziert, danach kompletter Suite-Lauf erneut grün.
  - Unabhängiger Review-Pass: CHANGES NEEDED → behoben. Reviewer fand, dass der Plan-Editor-Übung-Override (anders als das globale Gym-Feld) keinen `> 0`-Schutz beim Speichern hatte – ein getippter Wert wie "0" wäre unbehandelt als Faktor 0 persistiert worden, was bei künftiger Nutzung durch das noch offene PR/1RM-Ticket alle historischen Gewichte dieser Übung+Gym-Kombination still auf 0 umgerechnet hätte (kein Absturz, aber stille Daten-Korruption). Behoben durch denselben `> 0`-Schutz beim Speichern (0/negativ wird zu "kein Override" statt gespeichert) UND zusätzlich defensiv direkt in `GymConversion.convert` (beide Faktor-Seiten), plus 2 neue Regressionstests. Reviewer hat zusätzlich einen kleinen Zahlendreher im eigenen Test-Count-Zwischenstand des Autors bemerkt (keine Code-Auswirkung, nur eine falsche Arithmetik in der Status-Notiz).

- **Aktives Gym nach Löschung** – wenn das aktuell aktive Gym gelöscht wird, wird automatisch ein anderes Gym aktiv. *(2026-08-12)*
  - Gebaut: `GymActivation.promoteReplacement(afterDeleting:remaining:)` – no-op, wenn das gelöschte Gym gar nicht aktiv war oder keine Gyms übrig bleiben; sonst wird das alphabetisch erste verbleibende Gym aktiviert (deterministisch, passt zur ohnehin alphabetisch sortierten Gym-Liste). `GymListView`s Löschen-Swipe-Aktion ruft das vor `modelContext.delete`/`save` auf.
  - Getestet: 5 neue Unit-Tests für `GymActivation` (alphabetischer Tiebreak, genau ein verbleibendes Gym, keine verbleibenden Gyms crasht nicht, inaktives Gym löschen lässt andere unangetastet, fälschlich mehrfach aktive Gyms werden auf eins korrigiert) + 1 neuer XCUITest (aktives Gym löschen → verbleibendes Gym wird automatisch aktiv, geprüft über `XCUIElement.isSelected`). Gesamt 141 Unit-Tests + 13 UI-Tests grün.
  - Unabhängiger Review-Pass: APPROVE, keine blockierenden Findings. Reviewer hat den neuen UI-Test per Mutationsprobe (Fix temporär entfernt → Test schlägt exakt an der erwarteten Stelle fehl → Fix wiederhergestellt) als nicht-vakuos verifiziert und den Zustand "keine Gyms mehr vorhanden" in allen nachgelagerten `@Query`-Konsumenten (TrainingTabView, PlanEditorView, PlanExerciseEditView, WorkoutSessionBuilder) als bereits sicher nil-behandelt bestätigt – keine Regression durch dieses Ticket. Alphabetischer statt z. B. "zuletzt benutzt"-Tiebreak als bewusste, vertretbare Wahl notiert (nicht blockierend).

- **Statistik: Trainingsvolumen über Zeit** – pro Übung und gesamt. Nur `isCompleted == true`-Sätze zählen. *(2026-08-12)*
  - Gebaut: `TrainingVolume` (reine Business-Logik) summiert `weight × Wiederholungen` über alle erledigten Sätze pro zählbarer Session (`SessionStatistics.eligibleSessions`), optional gefiltert auf eine Übung. `exercisesWithVolumeHistory` liefert die Übungen mit echter Historie für den Picker, damit dieser nie eine Übung ohne Daten anbietet. Neue Sektion "Trainingsvolumen" im Statistik-Tab: Picker ("Gesamt" + Übungen mit Historie) über einem nativen Swift-Charts-Liniendiagramm (`.tint`-Farbe, eine Serie, daher bewusst keine Legende, wie in der `dataviz`-Skill-Anleitung für Ein-Serien-Charts).
  - Getestet: 9 Unit-Tests für `TrainingVolume` (leere Sessions, Gesamt- vs. Übungs-Filter, unerledigte Sätze ausgeschlossen, keine Sessions ohne relevante Sätze als Datenpunkt, nicht-zählbare Sessions ausgeschlossen, chronologische Sortierung, Übungsliste fürs Picker, Regressionstest für 0-Gewicht siehe unten) + 3 neue Assertions im bestehenden XCUITest `testCompletingAWorkoutShowsStatistics`. Gesamt 136 Unit-Tests + 12 UI-Tests grün.
  - **Bug gefunden und behoben**: `dataPoints` verwarf ursprünglich jeden Datenpunkt mit Volumen `0` (`guard volume > 0`), um Sessions ganz ohne relevante Sätze nicht als Null-Punkt zu zeigen – das traf aber fälschlich auch echte, erledigte Sätze mit (noch) 0 Gewicht (z. B. vergessene Eingabe oder künftige Bodyweight-Übungen) und ließ die komplette Sektion samt Picker und Chart verschwinden. Der eigene neue XCUITest (der beim Workout-Start standardmäßig kein Gewicht einträgt) deckte das direkt auf. Behoben durch Entfernen der `volume > 0`-Guard – die vorangehende "keine relevanten Sätze"-Prüfung reicht bereits aus, um echte von leeren Sessions zu unterscheiden; Regressionstest ergänzt.
  - Unabhängiger Review-Pass: CHANGES NEEDED → behoben. Reviewer hat den 0-Gewicht-Bug oben empirisch reproduziert (Testlauf schlug real fehl, nicht nur in der Theorie) und einen zweiten, nicht-blockierenden Fund gemacht: `@State private var selectedExercise: Exercise?` hielt eine rohe SwiftData-Modell-Referenz, die nach Löschen der gewählten Übung in einem anderen Tab veraltet gewesen wäre (Tab-State bleibt über `TabView` hinweg erhalten) – als einziger Fall im Projekt, der nicht wie sonst überall per `UUID` referenziert. Behoben durch `selectedExerciseID: UUID?` mit Lookup gegen die aktuelle Übungsliste bei jedem Zugriff (löst sich bei gelöschter Übung selbst auf). Kleinerer kosmetischer Fund (Singular/Plural "Trainingseinheit(en)" im VoiceOver-Text bei genau einem Datenpunkt) ebenfalls behoben.
  - Reviewer-Hinweis (nicht blockierend, für spätere Referenz): kein `AccessibilityChartDescriptor` für das Diagramm – einfaches Label+Value-Paar wird für den Umfang dieser Solo-Nutzer-App als ausreichend bewertet.

- **Statistik: meistgemachte Übungen** – Häufigkeit pro Übung. Nur `isCompleted == true`-Sätze zählen. *(2026-08-12)*
  - Gebaut: `ExerciseFrequency` zählt pro Übung, in wie vielen zählbaren Sessions (siehe `SessionStatistics.eligibleSessions`) sie mit mindestens einem erledigten Satz vorkam – mehrere Sätze derselben Übung an einem Tag zählen als 1, nicht als 5, um sich bewusst nicht mit dem künftigen "Trainingsvolumen"-Ticket zu überschneiden. Neue Sektion "Häufigste Übungen" im Statistik-Tab zeigt die Top 5, absteigend sortiert, bei Gleichstand alphabetisch.
  - Getestet: 7 Unit-Tests für `ExerciseFrequency` (Mehrfachsätze zählen einmal, Summierung über Sessions, unerledigte Sätze zählen nicht für ihre Übung auch wenn die Session durch eine andere Übung zählbar ist, nicht-zählbare Sessions komplett ausgeschlossen, Sortierung, alphabetischer Tiebreak) + bestehender XCUITest um zwei Assertions erweitert. Gesamt 127 Unit-Tests + 12 UI-Tests grün.
  - Unabhängiger Review-Pass: APPROVE, keine blockierenden Findings. Reviewer hat zwei rein kosmetische Randfälle notiert (nicht behoben, nicht blockierend): gleichnamige Übungen könnten bei Gleichstand pro App-Start unterschiedlich sortiert werden (hängt an der bereits akzeptierten fehlenden Namens-Eindeutigkeit); würde man alle Übungen mit Historie löschen, zeigt "Häufigste Übungen" leer während Dauer/Frequenz weiter Daten zeigen – kein Absturz, nur eine Inkonsistenz in einem sehr unwahrscheinlichen Szenario.

- **Statistik: Trainingsdauer & Frequenz** – durchschnittliche Trainingsdauer, Trainingsfrequenz pro Woche (Durchschnitt). Nur `isCompleted == true`-Sätze zählen. *(2026-08-12)*
  - Gebaut: erste echte Inhalte im bis dahin leeren Statistik-Tab. `SessionStatistics` (reine Business-Logik): eine Session zählt nur, wenn sie beendet ist (`endedAt != nil`) UND mindestens einen erledigten Satz hat – die Regel "nur erledigte Sätze zählen" auf Session-Ebene übersetzt, da Dauer/Frequenz Session- und keine Satz-Kennzahlen sind. Ø Trainings/Woche teilt durch mindestens eine volle Woche (nie durch null), damit ein einzelnes oder mehrere Trainings am selben Tag keine verzerrte Rate ergeben. `StatisticsTabView` zeigt "Ø Trainingsdauer" und "Ø Trainings / Woche" bzw. einen Empty State, solange kein Training abgeschlossen wurde.
  - Getestet: 12 Unit-Tests für `SessionStatistics` (Eligibility-Filter, Dauer-Mittelwert, Wochen-Frequenz inkl. Grenzfällen) + 1 neuer XCUITest (Training abschließen → Statistik-Tab zeigt beide Kennzahlen statt Empty State). Gesamt 120 Unit-Tests + 12 UI-Tests grün.
  - Unabhängiger Review-Pass: APPROVE, keine blockierenden Findings. Reviewer hat die Wochen-Grenzfall-Arithmetik (exakt 2 Wochen Spanne → 1.5/Woche) manuell auf Gleitkomma-Robustheit nachgerechnet und bestätigt, sowie verifiziert, dass ein nachträglich gelöschter einziger erledigter Satz einer bereits beendeten Session nicht erreichbar ist (es gibt noch keine Möglichkeit, in eine beendete Session zurückzukehren).
- **Plan duplizieren** – bestehenden Plan als Kopie übernehmen und nur Details ändern. U. a. hilfreich, um denselben Plan mit anderen Ziel-Gewichten für ein anderes Gym als Basis zu duplizieren (Gewichts-Vorschlag pro aktivem Gym übernimmt das aber für wiederkehrende Nutzung meist schon automatisch). *(2026-08-12)*
  - Gebaut: `TrainingPlanDuplication` (reine Kopier-Logik: neuer Plan mit "(Kopie)"-Suffix, alle Plan-Übungen mit gleichen Ziel-Werten/Notiz kopiert, gleiche `Exercise`-Referenzen statt Duplikate). Neue "Duplizieren"-Swipe-Aktion (führende Kante) in der Plan-Liste neben dem bestehenden "Löschen".
  - Getestet: 8 Unit-Tests für `TrainingPlanDuplication` + 1 neuer XCUITest (Plan mit Übung duplizieren → Original und Kopie beide sichtbar, Kopie enthält dieselbe Übung). Gesamt 109 Unit-Tests + 11 UI-Tests grün.
  - Unabhängiger Review-Pass: CHANGES NEEDED → behoben. Reviewer fand, dass kopierte Plan-Übungen einen frischen `updatedAt`-Zeitstempel statt des Original-Zeitstempels bekamen – das hätte `PlanExerciseDefaults.suggest` (wählt die zuletzt *getunte* Übung über alle Pläne hinweg) dazu bringen können, ein bloßes Duplikat fälschlich als "zuletzt benutzt" auszuwählen, obwohl es nie eigenständig bearbeitet wurde. Behoben durch Übernahme von `source.updatedAt`; empirisch mit einer Vorher/Nachher-Probe verifiziert. Reviewer hat außerdem die Swipe-Richtung (führende Aktion wird durch Wischen nach rechts sichtbar) und die Ziel-Gleichheit über einen echten Insert+Save+Fetch-Zyklus bestätigt.
- **Übung direkt im Plan-Editor erstellen** – im Übungs-Picker beim Hinzufügen einer Übung zu einem Plan soll man auch gleich eine neue, eigene Übung anlegen können, statt vorher zu Einstellungen → Übungen wechseln zu müssen. *(2026-08-12)*
  - Gebaut: `ExerciseFormView` bekommt einen optionalen `onCreate`-Callback (Standard `nil`, bestehender Aufrufer in der Übungsbibliothek unverändert). `ExercisePickerView` zeigt jetzt oben "Neue Übung erstellen", öffnet dafür `ExerciseFormView` als verschachteltes Sheet und übergibt die neu angelegte Übung direkt an denselben `onSelect`-Callback wie eine bestehende Auswahl – beide Sheets schließen sich in einem Schritt, man landet direkt im Plan-Editor mit der neuen Übung.
  - Getestet: 1 neuer XCUITest (Übung im Picker neu anlegen → landet direkt im Plan). Gesamt 100 Unit-Tests + 10 UI-Tests grün.
  - Unabhängiger Review-Pass: APPROVE. Reviewer hat den Dismiss-Mechanismus (zwei unabhängige, in Reihenfolge feuernde `dismiss()`-Aufrufe für Picker- und Formular-Sheet) live per Screenshot-Serie verifiziert – kein Flackern, kein hängendes Sheet.
- **Aufwärmsatz-Logik** – Gewicht wird automatisch als gestaffelter Prozentsatz des ersten Arbeitssatzes vorgeschlagen (z. B. 50/70/85 %), frei überschreibbar. Hinzufügen/Entfernen eines Aufwärmsatzes darf **nie** das Gewicht anderer Sätze verändern (RepCount-Bugfix). Unit-Tests explizit für diesen Fall. *(2026-08-12)*
  - Gebaut: `WarmupSuggestion` (gestaffelte Prozent-Ladder, 50/70/85 % für 3 Aufwärmsätze wie im Beispiel, Zwischenstufen konsistent als Präfix derselben Ladder statt unabhängig gerundeter Werte), `SetEntryOrdering` (reine `.order`-Neuindizierung, rührt nie Gewicht/Wdh./Notiz/Status an – analog zu `PlanExerciseOrdering`), `WarmupSetInsertion` (setzt einen neuen Aufwärmsatz vor den ersten Arbeitssatz derselben Übung, lässt alle anderen Übungen unangetastet). Neuer "Aufwärmsatz"-Button neben "Satz hinzufügen"; Aufwärmsätze bekommen ein Flammen-Icon in der Liste.
  - Getestet: 7 Tests für `WarmupSuggestion`, 3 für `SetEntryOrdering` (inkl. explizitem Test, dass Reindizierung wirklich nur `.order` verändert), 4 für `WarmupSetInsertion` (inkl. der vom Ticket geforderten Kernregression: Einfügen+Reindizieren verändert nie Gewicht/Wdh./Notiz/Status eines anderen Satzes) + 1 neuer XCUITest, der den kompletten Fluss (Arbeitssatz-Gewicht setzen → Aufwärmsatz hinzufügen → per Swipe löschen → Arbeitssatz-Gewicht unverändert) end-to-end durch die echte UI inkl. Swipe-Löschen (erstmals in diesem Projekt getestet) treibt. Gesamt 100 Unit-Tests + 9 UI-Tests grün.
  - Unabhängiger Review-Pass: APPROVE nach zwei durch Verbindungsabbrüche unterbrochenen Versuchen (Scratch-Dateien jeweils bemerkt/aufgeräumt) und einem dritten, abschließenden Durchgang. Reviewer hat den Double-Insertion-Fix (SwiftData verknüpft einen neuen `SetEntry` schon über den `session:`-Init-Parameter, vor explizitem Insert) empirisch bestätigt und einen kleinen Inkonsistenz-Fund gemacht: die 2-Aufwärmsätze-Ladder war unabhängig von der 3er-Ladder benannt (50/75 % statt 50/70 %) – behoben, damit inkrementelles Hinzufügen eine konsistente Progression ergibt, ohne je einen bereits erstellten Aufwärmsatz nachträglich zu verändern (das würde die Kernregel des Tickets selbst verletzen).
- **Workout beenden** – ein unaufdringlicher Hinweis bei nicht ausgefüllten Übungen/Sätzen ("X Übungen nicht ausgefüllt – entfernen?"), mit einem Tap erledigt. Nicht bestätigte/leere Einträge fließen nie in Statistiken/PRs ein. *(2026-08-12)*
  - Gebaut: `WorkoutCompletion.incompleteSets(in:)` findet alle nicht als "Erledigt" markierten Sätze einer Session. "Beenden" zeigt nur dann einen Alert ("X Satz/Sätze nicht ausgefüllt"), wenn welche existieren – ein Tap auf "Entfernen & Beenden" löscht sie und beendet, "Abbrechen" lässt die Session unverändert weiterlaufen. Granularität bewusst auf Satz- statt Übungsebene (das Datenmodell hat kein Übungs-Completion-Flag, eine Übung kann teilweise erledigt sein).
  - Getestet: 4 Unit-Tests für `WorkoutCompletion`, 2 neue XCUITests für den dritten und letzten von `CLAUDE.md` explizit genannten Kern-Flow ("Workout beenden") – mit Hinweis (nicht ausgefüllte Sätze vorhanden) und ohne (alle erledigt). Gesamt 86 Unit-Tests + 8 UI-Tests grün.
  - Beim Bau der Tests eine echte XCUITest-Eigenheit gefunden: SwiftUI's `Toggle` legt für Accessibility ein zeilengroßes äußeres "Switch"-Element (das eigentlich gematchte Label) UM das eigentliche, unbeschriftete innere Switch-Widget – Tippen auf das äußere Element schaltet den echten Schalter nicht um. Live im Simulator reproduziert und mit `toggle.switches.firstMatch.tap()` (auf das innere Element scopen) gelöst; für künftige Toggle-Interaktionen in Tests relevant.
  - Unabhängiger Review-Pass: APPROVE, keine blockierenden Findings. Reviewer hat den Toggle-Fund durch bewusstes Zurücksetzen und Reproduzieren des Fehlers verifiziert. Ein Sicherheitsaspekt festgehalten (siehe neuer Backlog-Punkt "Schnelles Erledigt-Markieren").
- **Workout starten & normale Sätze loggen** – Workout aus einem Plan starten, Sätze mit Gewicht/Wiederholungen loggen. Temporäre Notiz pro geloggtem Satz (`SetEntry.note`) editierbar machen; permanente Ausrüstungs-Notiz (Sitz/Griff) automatisch vorschlagen wie das Gewicht. *(2026-08-12)*
  - Gebaut: der Training-Tab hat jetzt echten Inhalt. `WorkoutSessionBuilder` (reine Business-Logik: baut aus einem Plan eine Session + vorbefüllte Sätze, nutzt `SetSuggestion` fürs aktive Gym, sonst Plan-Zielwerte, sonst 3×10/0). `TrainingTabView` (Plan zum Starten wählen, oder laufende Session direkt fortsetzen), `WorkoutSessionView` (Sätze nach Übung gruppiert, Tap zum Bearbeiten, Swipe-Löschen, Satz hinzufügen, "Beenden"), `SetEditView` (Wiederholungen/Gewicht/Notiz/Erledigt-Status, permanente Ausrüstungs-Notiz fürs Gym wie in `PlanExerciseEditView`). "Workout beenden" ist hier bewusst nur `endedAt = .now` – die intelligente Hinweis-Logik für unausgefüllte Sätze ist ihr eigenes, noch offenes Ticket.
  - Getestet: 10 Tests für `WorkoutSessionBuilder`, 7 für die neu extrahierte `WeightInput`-Parsing-Logik, plus der zentrale User-Flow "Workout starten & Satz loggen" als XCUITest (von `CLAUDE.md` explizit als Kern-Flow gefordert) inkl. Regressionstest fürs Gewichts-Feld. Gesamt 82 Unit-Tests + 6 UI-Tests grün.
  - **Echter Bug gefunden und behoben** (höchste Priorität bisher, nahe am RepCount-Bug, den dieses Projekt explizit vermeiden soll): mehrstelliges Gewicht eintippen (z. B. "55") konnte sich selbst korrumpieren (wurde zu "5.05"), weil eine berechnete `Binding<String>` nach jedem Tastendruck einen neu formatierten Wert zurück ins Live-Textfeld schrieb und dabei mit dem Cursor kollidierte – vom Reviewer live im Simulator reproduziert. Behoben durch reinen `@State`-Text ohne Rückformatierung; Parsing in eine eigene, direkt unit-getestete `WeightInput`-Funktion ausgelagert. Derselbe Fix wurde vorsorglich auch in `PlanExerciseEditView` angewendet (identisches Muster, dort mit `Double?`-Semantik statt `?? 0`).
  - Unabhängiger Review-Pass: zwei Versuche brachen durch Verbindungsabbrüche mitten im Review ab und hinterließen Scratch-Dateien (jeweils bemerkt und aufgeräumt); dritter Versuch kam durch und bestätigte APPROVE nach eigenem Build+Test-Lauf und Code-Review des Fixes. Dabei lief ein `rm -rf` auf `GymTrack.xcodeproj` (Sicherheitswarnung ausgelöst) – verifiziert harmlos, da der Ordner per `.gitignore` bewusst nicht versioniert und jederzeit mit `xcodegen generate` neu erzeugbar ist; Projekt neu generiert, danach nochmals vollständiger Build+Test-Lauf grün.
- **Gewichts- & Wiederholungs-Vorschlag pro Übung+Gym** – Business-Logik: letztes Gewicht UND letzte Wiederholungszahl pro (Übung, Gym) aus der Satz-Historie ableiten, beim nächsten Training in diesem Gym automatisch vorschlagen (kein neues Datenmodell nötig, `SetEntry` hat `weight`/`reps`/`exercise`/`gym`/`session.startedAt` bereits). Unit-Tests für die Vorschlagslogik. *(2026-08-11)*
  - Gebaut: `SetSuggestion.suggest(for:gym:from:)` – findet den letzten abgeschlossenen normalen Satz (kein Aufwärmsatz, kein Dropset) für (Übung, Gym), sortiert nach Session-Startzeit, innerhalb derselben Session nach höchster Satz-Order. Noch von keiner UI konsumiert (Workout-Logging-Ticket kommt erst noch) – bewusst reine Business-Logik vorab, analog zu `PlanExerciseDefaults`.
  - Getestet: 8 Unit-Tests (keine Historie, einfacher Treffer, falsche Übung/falsches Gym ignoriert, unvollständige Sätze ignoriert, Aufwärmsatz/Dropset ignoriert, wählt jüngste Session, Tiebreak nach höchster Order innerhalb einer Session). Gesamt 65 Unit-Tests + 4 UI-Tests grün.
  - Unabhängiger Review-Pass: APPROVE, keine Findings. Reviewer hat Build-Zeit und Type-Checking explizit mit aggressiven Warnschwellen gegengeprüft (SourceKit hatte fälschlich "unable to type-check" auf eine Filter-Closure gemeldet, die im echten Build unauffällig war) und alle 8 Tests per Mutationstest als nicht-vakuos bestätigt.

- **Plan-Editor: freie Erstellung** – Übungen aus der Bibliothek hinzufügen, Drag & Drop-Neuanordnung, Sätze/Wiederholungen/Gewicht mit sinnvollen Default-Werten (letzte genutzte Werte oder 3×10) vorausgefüllt. Notizfeld pro Plan-Übung (`PlanExercise.note`) editierbar machen; permanente Ausrüstungs-Notiz (Sitz/Griff) pro Übung+Gym anzeigen/editierbar machen. *(2026-08-11)*
  - Gebaut: `PlanExerciseDefaults` (schlägt Sätze/Wdh./Gewicht aus dem zuletzt getunten `PlanExercise` für diese Übung vor, sonst 3×10), `PlanExerciseOrdering` (weist nach Drag/Löschen lückenlose `order`-Werte neu zu), `ExerciseGymNoteStore` (Suchen-oder-Anlegen für die permanente Ausrüstungs-Notiz, verhindert Duplikate). `PlanListView` (Pläne-Tab, "+" legt Plan an und springt direkt in den Editor), `PlanEditorView` (Name, Übungsliste mit Tap-zum-Bearbeiten/Swipe-Löschen/Drag-Reorder via `EditButton`, "+" öffnet Übungs-Picker), `ExercisePickerView` (Sheet, wiederverwendet die Gruppierung/Suche aus der Übungsbibliothek), `PlanExerciseEditView` (Sätze/Wdh.-Stepper, Gewicht, Plan-Notiz, permanente Ausrüstungs-Notiz fürs aktive Gym). Pläne-Tab zeigt jetzt `PlanListView` statt Platzhalter.
  - Getestet: 5 Tests für `PlanExerciseDefaults`, 3 für `PlanExerciseOrdering`, 3 für `ExerciseGymNoteStore`, 3 weitere für `ExerciseSearch.grouped` (aus der Übungsbibliothek herausgezogen, jetzt von `ExerciseLibraryView` und `ExercisePickerView` gemeinsam genutzt) + 1 neuer XCUITest für den zentralen Flow "Plan erstellen → Übung hinzufügen" (explizit von `CLAUDE.md` als Kern-Flow gefordert). Gesamt 57 Unit-Tests + 4 UI-Tests grün, Build+Tests liefen diesmal beim ersten Versuch komplett durch.
  - Unabhängiger Review-Pass: CHANGES NEEDED → behoben. Reviewer fand, dass `PlanExerciseEditView.save()` beim reinen Sätze/Wdh./Gewicht-Tuning nie den Zeitstempel bumpte, wodurch "letzte genutzte Werte" über die falsche (Plan- statt Übungs-)Aktualität sortiert hätte. Behoben durch eigenes `PlanExercise.updatedAt`-Feld (statt sich auf `plan.updatedAt` zu verlassen) – sauberer als der ursprünglich vorgeschlagene Minimal-Fix, da jetzt unabhängig von unbeteiligten Bearbeitungen im selben oder einem anderen Plan. Reviewer hat Drag-Reorder und Swipe-Löschen im Simulator live durchgespielt (nicht nur Code gelesen) und beides funktionierend bestätigt.

- **Übungsbibliothek** – vordefinierte Grundmenge an Standardübungen, durchsuchbar, eigene Übungen jederzeit ergänzbar. *(2026-08-11)*
  - Gebaut: `ExerciseSeeder` mit 34 vordefinierten deutschen Standardübungen (idempotent, seedet einmalig beim App-Start, rührt bestehende/eigene Übungen nie an); `ExerciseSearch` als reine, testbare Filter-Funktion (case-/diakritik-insensitiv); `MuscleGroup.displayName` für deutsche UI-Labels; `ExerciseLibraryView` (durchsuchbare, nach Muskelgruppe gruppierte Liste, Swipe-Löschen, zwei getrennte Empty-States für "keine Übungen" vs. "keine Treffer") + `ExerciseFormView` (eigene Übung anlegen: Name + Muskelgruppe). Einstellungen-Tab hat jetzt "Übungen" neben "Gyms".
  - Getestet: 5 Tests für `ExerciseSeeder` (leerer Store, doppeltes Seeden, Name-Kollision, eigene Übungen bleiben unangetastet, keine Duplikate in der Default-Liste selbst), 5 Tests für `ExerciseSearch`, 2 Tests für `MuscleGroupDisplay` (jede Gruppe hat einen nicht-leeren, eindeutigen Namen) + 1 neuer XCUITest (eigene Übung anlegen erscheint in der Liste). Gesamt 43 Unit-Tests + 3 UI-Tests grün.
  - Zwei UI-Test-Bugs gefunden & behoben: (1) Neue Übung landet in der letzten Sektion ("Sonstiges") und ist bei ~35 Einträgen off-screen – SwiftUIs `List` rendert nur sichtbare Zellen, `waitForExistence` lief ins Leere. (2) Versuch, stattdessen über die Suchleiste zu filtern, scheiterte ebenfalls – `app.searchFields` fand kein Element, weil `.searchable` auf einer per `NavigationLink` gepushten (nicht Root-)View ihr Suchfeld offenbar erst bei tatsächlichem Rendern/Scrollen materialisiert. Gelöst durch Scroll-Schleife statt Scroll- oder Suchfeld-Annahmen.
  - Unabhängiger Review-Pass: APPROVE. Reviewer merkte an, dass `ExerciseRow` (anders als `GymRow`) kein `.accessibilityElement(children: .combine)` nutzte – für VoiceOver-Konsistenz nachgezogen, inkl. stabiler `.accessibilityIdentifier` und robusterer UI-Test-Abfrage (`descendants(matching: .any)` statt typgebundener Query). Danach erneut Build+Tests grün.

- **Gym-Verwaltung (UI)** – Gyms anlegen/bearbeiten/löschen (Name, optionale Notiz), ein Gym als aktuell aktiv markieren. *(2026-08-11)*
  - Gebaut: erste echte UI-Ticket des Projekts. `GymActivation` (ViewModels/) als pure, testbare Business-Logik ("genau ein Gym aktiv"); `GymListView` (Liste mit Tap-zum-Aktivieren, Swipe-Löschen, Swipe-Bearbeiten, Empty State) + `GymFormView` (Anlegen/Bearbeiten-Sheet, Name Pflicht, Notiz optional, getrimmt, leere Notiz wird zu `nil`); Einstellungen-Tab in `ContentView` verlinkt jetzt zu Gyms statt Platzhaltertext. `--uitesting`-Launch-Flag sorgt dafür, dass UI-Tests den In-Memory-Container statt des echten CloudKit-Stores benutzen (keine Verschmutzung echter Daten, keine Flakiness durch alte Testdaten).
  - Getestet: 6 neue Unit-Tests für `GymActivation` (Deaktivieren anderer, bereits-aktiv bleibt aktiv, einzelnes Gym, leere Liste, Ziel nicht in Liste, mehrere fälschlich aktive Gyms werden auf eins korrigiert) + 1 neuer XCUITest (Gym anlegen erscheint in der Liste). Gesamt 31 Unit-Tests + 2 UI-Tests grün.
  - Bug gefunden & behoben: Der neue UI-Test schlug erst fehl, weil `GymRow` per `.accessibilityElement(children: .combine)` (für VoiceOver) den Namen-Text in das Label des umschließenden Buttons verschmilzt – `app.staticTexts["Test-Gym"]` fand daher nichts. Auf `app.buttons[...]` umgestellt.
  - Unabhängiger Review-Pass: APPROVE. Reviewer fand zusätzlich, dass der kombinierte Button-Label bei Gyms **mit Notiz** zu `"Name, Notiz"` wird – künftige Tests, die nur nach dem Namen suchen, würden das nicht mehr finden. Behoben durch zusätzliche stabile `.accessibilityIdentifier(gym.name)` auf dem Button, unabhängig vom VoiceOver-Label. Danach erneut Build+Tests grün.
  - Bekannte, akzeptierte Lücke: Löschen des aktiven Gyms lässt kein Gym aktiv (kein Auto-Promote) – als eigener Backlog-Punkt unter "Offen" festgehalten.

- **Datenmodell-Erweiterung: Temporäre Erinnerungs-Notiz** – neue Entity für eine Notiz pro (Übung, Gym), die einmalig beim nächsten Training dieser Übung in genau diesem Gym angezeigt und danach automatisch nicht mehr angezeigt wird (Anzeige-/Konsum-Logik selbst ist Teil der späteren Workout-Logging-UI, hier nur das Datenmodell). *(2026-08-11)*
  - Gebaut: neue Entity `ExerciseGymReminder` (Exercise+Gym → `text`, `isConsumed`-Flag), cascade-delete auf beiden Seiten wie bei `ExerciseGymNote`. Das eigentliche "beim nächsten Training anzeigen und danach ausblenden" ist bewusst nicht Teil dieses Tickets – das ist Business-/UI-Logik einer späteren Workout-Logging-Aufgabe, hier nur die Datengrundlage (Text + Konsum-Status + Ziel).
  - Getestet: 5 neue XCTest-Fälle (Defaults, Cascade-Delete bei Exercise-Löschung, Cascade-Delete bei Gym-Löschung, Geschwister-Isolation zwischen zwei Gyms, mehrere gleichzeitig offene Reminder für dieselbe Übung+Gym-Kombination möglich da keine Unique-Constraint). Gesamt 25 Unit-Tests + 1 UI-Test grün.
  - Unabhängiger Review-Pass: APPROVE, keine Findings. Reviewer hat den Cascade-Fix durch eine Mutationsprobe verifiziert (Inverse entfernt → Crash reproduziert → zurückgesetzt → wieder grün) und gegen `ExerciseGymNote` auf Konsistenz geprüft.

- **Datenmodell-Erweiterung: Notizen** – dauerhafte Ausrüstungs-Notiz pro Übung+Gym (z. B. Sitzposition, Griff), immer automatisch abrufbar wie das Gewicht; zusätzlich temporäre Notizfelder pro Plan-Übung (`PlanExercise.note`) und pro geloggtem Satz (`SetEntry.note`). *(2026-08-11)*
  - Gebaut: neue Entity `ExerciseGymNote` (Exercise+Gym → permanente Notiz), cascade-delete auf beiden Seiten (Notiz ohne Übung oder Gym ist bedeutungslos); `note`-Feld auf `PlanExercise` und `SetEntry` für temporäre, instanzgebundene Notizen. Kein neues Datenmodell für "Gewicht+Wiederholungen merken" nötig – dafür reicht die vorhandene Satz-Historie, betrifft nur die Business-Logik einer späteren Ticket-Umsetzung (siehe "Gewichts- & Wiederholungs-Vorschlag pro Übung+Gym" unter Offen).
  - Getestet: 6 neue XCTest-Fälle (Defaults/Settability für beide Notiz-Felder, Cascade-Delete bei Exercise-Löschung, Cascade-Delete bei Gym-Löschung, Geschwister-Isolation zwischen zwei Gyms). Gesamt 20 Unit-Tests + 1 UI-Test grün, inkl. der bestehenden `testSchemaIsCloudKitCompatible`, die die neue Entity automatisch mit abdeckt.
  - Unabhängiger Review-Pass: APPROVE, keine Findings.

- **Datenmodell (SwiftData)** – Gym, Exercise, TrainingPlan, PlanExercise, WorkoutSession, SetEntry, PersonalRecord inkl. Relationships/Delete-Rules, CloudKit-kompatibel; Persistence-Layer + minimaler App-Einstieg. *(2026-08-11)*
  - Gebaut: 7 SwiftData-`@Model`-Typen mit korrekten Cascade/Nullify-Delete-Rules (TrainingPlan→PlanExercise und WorkoutSession→SetEntry cascadieren, Gym/Exercise-Rückreferenzen nullifizieren statt zu löschen), Enum-Persistenz über Raw-Value-Strings (`MuscleGroup`, `SetType`) mit Fallback auf Default bei unbekanntem Rohwert, `PersistenceController` (echter CloudKit-Container + In-Memory-Variante für Tests), minimaler `GymTrackApp`/`ContentView`-Einstieg mit Tab-Skeleton.
  - Getestet: 14 XCTest-Unit-Tests (Defaults/0-Werte, Enum-Rundtrip inkl. unbekannter Rohwerte, Cascade-Delete für Plan→PlanExercise und Session→SetEntry, Nullify bei Exercise-Löschung, 50-Sätze-Stresstest, explizite CloudKit-Schema-Validierung mit aktiviertem `cloudKitDatabase`) + 1 XCUITest (App-Start zeigt TabBar). Build & Tests laufen grün auf iPhone 17 Simulator (iOS 26.5).
  - Bug gefunden & behoben: Erster Testlauf crashte beim App-Start, weil `Exercise`/`Gym` für mehrere Relationships keine explizite Inverse hatten (SwiftData+CloudKit-Pflicht). Behoben durch `@Relationship(inverse:)`-Rückreferenzen auf beiden Seiten; Regressionstest `testSchemaIsCloudKitCompatible` verhindert, dass das unbemerkt wieder passiert (der reine In-Memory-Container ohne CloudKit hätte den Fehler nicht gefunden).
  - Unabhängiger Review-Pass (frischer Subagent): APPROVE, keine blockierenden Findings. Reviewer hat den Inverse-Fix live durch eine Mutationsprobe verifiziert (Inverse entfernt → exakt derselbe Crash reproduziert → zurückgesetzt → wieder grün).
